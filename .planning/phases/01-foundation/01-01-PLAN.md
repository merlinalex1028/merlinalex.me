---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - astro.config.mjs
  - tsconfig.json
  - .nvmrc
  - .gitignore
  - .env.example
  - src/env.d.ts
  - src/content.config.ts
  - src/content/articles/welcome.md
  - src/content/projects/.gitkeep
  - src/content/creations/.gitkeep
  - src/content/microblog/.gitkeep
  - src/content/timeline/.gitkeep
  - src/content/friends/friends.json
  - src/content/anime/list.json
  - src/content/books/list.json
  - src/content/music/list.json
  - src/data/persona.yaml
  - src/data/notice.md
  - src/data/social.json
  - src/data/friends-health.json
  - src/styles/global.css
  - src/pages/index.astro
  - public/robots.txt
  - public/favicon.svg
  - public/og-default.svg
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
must_haves:
  truths:
    - "A visitor can run `pnpm dev` and see a styled Hello-World placeholder on localhost — the Walking Skeleton baseline is up"
    - "9 Zod-validated content collections (6 detailed + 3 placeholder) live in `src/content.config.ts` at the Astro 6 path and validate at build time — INFRA-02, D-01"
    - "Article schema enforces required `title`, `publishedAt`, `tags[]` (default []), `draft` (default false) and accepts optional `description`, `updatedAt`, `cover`, `category`, `sticky`, `password`, `toc` per D-02"
    - "Friends, Timeline, Microblog, Projects, Creations schemas match D-03 / D-04 / D-05 / D-06 / D-07 field-by-field"
    - "Anime, Books, Music placeholders use empty `z.object({ items: z.array(z.object({})).default([]) })` per D-01 — refined in Phase 4"
    - "Project is initialized with Astro v6.4.2 + TypeScript strict + Tailwind v4 via `@tailwindcss/vite` (NOT `@astrojs/tailwind`) + MDX + Sitemap + pnpm + Node 22 LTS per D-13 / INFRA-01"
    - "`pnpm build` exits 0; `dist/index.html` exists; the dev server starts on a free port and serves the scaffolded placeholder"
    - "All 7 data + content seed files are present (persona.yaml, notice.md, social.json, friends-health.json, friends.json, anime/books/music list.json) with the shapes required by downstream plans"
  artifacts:
    - path: "package.json"
      provides: "Pinned Astro 6.4.2 + @tailwindcss/vite 4.3.0 + @astrojs/mdx 6.0.1 + @astrojs/sitemap 3.7.3 + zod 4.4.3 + engines.node '>=22.0.0'"
      min_lines: 30
    - path: "astro.config.mjs"
      provides: "Astro 6 config with @tailwindcss/vite in vite.plugins (NOT integrations) + mdx() + sitemap() + site: 'https://merlinalex.me'"
      contains: "@tailwindcss/vite"
    - path: "src/content.config.ts"
      provides: "9 Zod-validated content collections with `glob` (markdown) + `file` (JSON) loaders"
      contains: "export const collections"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 import + :root[data-theme=\"light\"] tokens + base body styles (dark + reduced-motion + atmo gate added in plan 01-02)"
      contains: "@import \"tailwindcss\""
    - path: "src/pages/index.astro"
      provides: "Hello-World placeholder that uses BaseLayout-less layout so plan 01-02 can swap it for the real Home"
    - path: "src/data/persona.yaml"
      provides: "All D-14 fields present (avatar path, name.zh, name.romaji, mbti, zodiac, blood, tagline, skills[], favorites.anime[], favorites.characters[], qa[]) with sensible placeholder values"
      contains: "tagline:"
    - path: "public/robots.txt"
      provides: "User-agent: * / Allow: / / Sitemap: https://merlinalex.me/sitemap-index.xml"
      contains: "Sitemap:"
  key_links:
    - from: "src/content.config.ts"
      to: "src/content/{articles,projects,...}/*"
      via: "glob({ base, pattern }) and file() loader arguments"
      pattern: "loader: glob\\("
    - from: "astro.config.mjs"
      to: "vite build pipeline"
      via: "@tailwindcss/vite plugin"
      pattern: "plugins: \\[tailwindcss\\(\\)\\]"
    - from: "src/styles/global.css"
      to: "Tailwind v4"
      via: "@import \"tailwindcss\" directive"
      pattern: "@import \\\"tailwindcss\\\""
---

<objective>
Bootstrap a greenfield Astro 6.4.2 project on Cloudflare Pages with the full Phase 1 stack wired: TypeScript strict, Tailwind v4 via `@tailwindcss/vite`, MDX, Sitemap, pnpm, Node 22 LTS, Zen Maru Gothic + JetBrains Mono fonts, `astro-icon` integration, and the Vitest + Playwright test runners scaffolded (no tests yet). Define all 9 Zod-validated content collections (6 detailed per D-02..D-07 + 3 placeholder per D-01) at the Astro 6 path (`src/content.config.ts`, NOT `src/content/config.ts`) and seed them with the empty-but-valid shapes downstream plans consume. Establish the `src/data/`, `src/styles/`, `public/` directories and their initial content.

Purpose: This is the Walking Skeleton. The deliverable is a runnable dev server with a Hello-World placeholder page AND a build-time-validating content schema graph — every later plan in this phase builds on this baseline.

Output: A greenfield Astro 6 project that runs `pnpm dev` and `pnpm build` cleanly with no consumers yet, ready for plan 01-02 to drop in the theme/atmo gate and BaseLayout.

<user_story>
**As a** visitor, **I want to** load a styled scaffolded site on a custom domain with no errors, **so that** I have a stable kawaii baseline to explore the personal site as content is added in later phases.
</user_story>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huangjingping/i/merlinalex.me/CLAUDE.md
@/Users/huangjingping/i/merlinalex.me/.planning/STATE.md
@/Users/huangjingping/i/merlinalex.me/.planning/ROADMAP.md
@/Users/huangjingping/i/merlinalex.me/.planning/REQUIREMENTS.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Astro 6 project + install Phase 1 dependencies</name>
  <files>package.json, pnpm-lock.yaml, tsconfig.json, .nvmrc, astro.config.mjs, src/env.d.ts]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-RESEARCH.md §Standard Stack lines 92-185 (verified package versions)
  - .planning/phases/01-foundation/01-RESEARCH.md §Installation lines 137-155 (exact install commands + caveats)
  - .planning/phases/01-foundation/01-PATTERNS.md §astro.config.mjs (config shape + invariants)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 3 lines 729-745 (NEVER install @astrojs/tailwind)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 4 lines 746-758 (config at src/content.config.ts NOT src/content/config.ts)
  </read_first>
  <action>
  Run `pnpm create astro@latest . --template minimal --typescript strict --no-git --install --skip-houston --yes` from `/Users/huangjingping/i/merlinalex.me/`. This scaffolds Astro 6 with TypeScript strict, creates `package.json`, `tsconfig.json` (extends `astro/tsconfigs/strict`), `astro.config.mjs`, `src/env.d.ts`, and a minimal `src/pages/index.astro`. Then run the integration commands in this exact order: `pnpm astro add tailwind`, `pnpm astro add mdx`, `pnpm astro add sitemap`, `pnpm astro add icon`. Then install the Phase 1 supporting deps: `pnpm add @fontsource/zen-maru-gothic @fontsource/jetbrains-mono`, then `pnpm add -D vitest @playwright/test`. After all installs, manually verify and fix `astro.config.mjs` so that (a) it imports `tailwindcss` from `@tailwindcss/vite` and registers it via `vite.plugins: [tailwindcss()]` — NOT via `integrations: [tailwind()]`; (b) `site: 'https://merlinalex.me'` is set (required for `@astrojs/sitemap`); (c) `integrations: [mdx(), sitemap()]` is the list (icon integration configures itself automatically). Add `trailingSlash: 'never'` and `build: { inlineStylesheets: 'auto' }` for production-friendly URLs. Create `.nvmrc` with a single line containing `22`. Update `package.json` so `engines.node` equals `">=22.0.0"`. Update `.gitignore` to include `.env`, `.env.local`, `.env.*.local`, `node_modules`, `dist`, `.astro`, `.DS_Store`, `*.log`, and `.cache` (Astro scaffold's default gitignore is missing some of these). Verify the package.json has `@tailwindcss/vite` (NOT `@astrojs/tailwind`) and that versions match the locked list: `astro@^6.4.2`, `@tailwindcss/vite@^4.3.0`, `@astrojs/mdx@^6.0.1`, `@astrojs/sitemap@^3.7.3`, `zod@^4.4.3`, `tailwindcss@^4.3.0`. Do not run `pnpm add -D @astrojs/tailwind` under any circumstance — that is the legacy Tailwind 3 integration and breaks v4.
  </action>
  <verify>
  <automated>pnpm -s ls --depth 0 2>/dev/null | grep -E "(@astrojs/core|@astrojs/sitemap|@astrojs/mdx|@tailwindcss/vite|@tailwindcss/vite|tailwindcss|zod|astro)@" | head -20; pnpm -s ls --depth 0 2>/dev/null | grep -c "@astrojs/tailwind" | grep -v '^#' | (read n; test "$n" = "0" && echo "OK: no @astrojs/tailwind installed")</automated>
  </verify>
  <acceptance_criteria>
  - `package.json` exists at `/Users/huangjingping/i/merlinalex.me/package.json` and contains `"astro": "^6.4.2"` in dependencies
  - `package.json` contains `"@tailwindcss/vite": "^4.3.0"` and `"tailwindcss": "^4.3.0"` in dependencies
  - `package.json` contains `"@astrojs/mdx": "^6.0.1"` and `"@astrojs/sitemap": "^3.7.3"` in dependencies
  - `package.json` does NOT contain `"@astrojs/tailwind"` anywhere (grep returns 0 matches)
  - `package.json` `engines.node` field is `">=22.0.0"`
  - `pnpm-lock.yaml` exists at repo root (committed lockfile)
  - `.nvmrc` exists with single line `22`
  - `astro.config.mjs` exists and contains `import tailwindcss from '@tailwindcss/vite'`
  - `astro.config.mjs` contains `site: 'https://merlinalex.me'`
  - `astro.config.mjs` contains `vite: {` ... `plugins: [tailwindcss()]` (NOT in `integrations: []`)
  - `tsconfig.json` extends `"astro/tsconfigs/strict"`
  - `.gitignore` contains `.env`, `.env.local`, `node_modules`, `dist`, `.astro`, `.DS_Store`
  - `pnpm install` exits 0
  </acceptance_criteria>
  <done>Astro 6 project scaffolded with all required dependencies, configs, and the correct plugin placement (@tailwindcss/vite in vite.plugins, NOT in integrations). pnpm-lock.yaml is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Create `src/content.config.ts` with 9 Zod-validated content collections</name>
  <files>src/content.config.ts]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-RESEARCH.md §Pattern 2 lines 397-520 (full 9-collection example)
  - .planning/phases/01-foundation/01-PATTERNS.md §src/content.config.ts (corrected import paths + critical Astro 6 changes)
  - .planning/phases/01-foundation/01-RESEARCH.md §Anti-patterns 1-3 lines 658-666 (forbidden: src/content/config.ts path; astro:content z import; entry.render() / .slug)
  - .planning/phases/01-foundation/01-RESEARCH.md §State of the Art lines 1050-1066 (Astro 6 import changes: astro/zod, render(entry), getEntry, entry.id)
  - .planning/phases/01-foundation/01-CONTEXT.md §Decisions D-01 through D-07 (per-collection field list)
  </read_first>
  <action>
  Create `src/content.config.ts` (Astro 6 path, NOT `src/content/config.ts` — the v5 path throws `LegacyContentConfigError` in Astro 6). Import `defineCollection` from `astro:content`, `z` from `astro/zod` (NOT from `astro:content` — that import is deprecated in Astro 6), and `glob, file` from `astro/loaders`. Declare exactly 9 collections, exporting them as a single `collections` object. The 6 detailed collections (D-01) and their field shapes per CONTEXT D-02..D-07:

  1. `articles` — loader `glob({ base: './src/content/articles', pattern: '**/*.{md,mdx}' })`. Schema: `title: z.string()`, `publishedAt: z.coerce.date()`, `tags: z.array(z.string()).default([])`, `draft: z.boolean().default(false)`, `description: z.string().optional()`, `updatedAt: z.coerce.date().optional()`, `cover: z.string().optional()`, `category: z.string().optional()` (freeform per D-02), `sticky: z.boolean().default(false)`, `password: z.string().optional()`, `toc: z.boolean().default(true)`.
  2. `projects` — loader `glob` over `./src/content/projects`. Schema: `name: z.string()`, `url: z.string().url()`, `description: z.string().optional()`, `tags: z.array(z.string()).default([])`, `github: z.string().url().optional()`, `featured: z.boolean().default(false)`, `cover: z.string().optional()`.
  3. `creations` — loader `glob` over `./src/content/creations`. Schema: `title: z.string()`, `publishedAt: z.coerce.date()`, `images: z.array(z.string()).min(1)`, `description: z.string().optional()`, `tags: z.array(z.string()).default([])`, `cover: z.string().optional()`, `category: z.enum(['illustration', 'photography', 'craft', 'video']).optional()`.
  4. `microblog` — loader `glob` over `./src/content/microblog` with `pattern: '**/*.md'`. Schema: `publishedAt: z.coerce.date()`, `content: z.string()`, `images: z.array(z.string()).default([])`, `mood: z.string().optional()`, `tags: z.array(z.string()).default([])`. The auto-archive >180d flag is derived at render layer per D-05 — DO NOT add an `archived` field to the schema.
  5. `friends` — loader `file('./src/content/friends/friends.json')`. Schema: `items: z.array(z.object({ name: z.string(), url: z.string().url(), avatar: z.string().url().optional(), description: z.string().optional(), category: z.enum(['tech', 'anime', 'life', 'other']).optional(), featured: z.boolean().default(false) })).default([])`.
  6. `timeline` — loader `glob` over `./src/content/timeline` with `pattern: '**/*.md'`. Schema: `date: z.coerce.date()`, `title: z.string()`, `description: z.string().optional()`, `image: z.string().optional()`, `link: z.string().url().optional()`, `side: z.enum(['left', 'right', 'auto']).default('auto')`.
  7-9. `anime`, `books`, `music` — placeholder per D-01, all using `file` loader pointing at `./src/content/<name>/list.json`, schema `z.object({ items: z.array(z.object({})).default([]) })`. Refined in Phase 4.

  All date fields use `z.coerce.date()` (handles both ISO strings and Date objects from YAML/MDX frontmatter). All URL fields use `z.string().url()` to validate at build time. All array fields use `.default([])` — NEVER `.optional()`. All boolean fields with semantic default use `.default(false)` or `.default(true)` per field. The final line exports `export const collections = { articles, projects, creations, microblog, friends, timeline, anime, books, music };`.
  </action>
  <verify>
  <automated>test -f src/content.config.ts && grep -c "loader: glob\\|loader: file" src/content.config.ts | (read n; test "$n" -ge 9 && echo "OK: 9+ loaders")</automated>
  </verify>
  <acceptance_criteria>
  - File exists at `/Users/huangjingping/i/merlinalex.me/src/content.config.ts` (NOT `src/content/config.ts`)
  - File imports `defineCollection` from `astro:content`
  - File imports `z` from `astro/zod` (NOT from `astro:content`)
  - File imports `glob, file` from `astro/loaders`
  - File contains `export const collections = {` and lists all 9 keys: `articles, projects, creations, microblog, friends, timeline, anime, books, music`
  - The `articles` schema defines all fields from D-02: `title` (required), `publishedAt` (required, `z.coerce.date()`), `tags` (default `[]`), `draft` (default `false`), and all 8 optional fields
  - The `friends` schema wraps an `items` array per the `file()` loader pattern; the items object has `name` (required), `url` (required, `.url()`), and all 4 optional fields per D-03
  - The `microblog` schema has `publishedAt` (required) and `content` (required) per D-05; no `archived` field
  - The `creations` schema has `category: z.enum(['illustration', 'photography', 'craft', 'video']).optional()` per D-07
  - The `timeline` schema has `side: z.enum(['left', 'right', 'auto']).default('auto')` per D-04
  - The `anime`, `books`, `music` schemas are identical placeholders: `z.object({ items: z.array(z.object({})).default([]) })`
  - No imports use `import { z } from 'astro:content'` (deprecated path) — verified by grep returning 0
  - No file lives at `src/content/config.ts` (Astro 5 path that would throw `LegacyContentConfigError`)
  </acceptance_criteria>
  <done>src/content.config.ts defines all 9 Zod-validated collections using the corrected Astro 6 imports (`astro/zod`, `astro/loaders`) and the right loader types per collection (glob for markdown, file for JSON). Schemas match CONTEXT D-02..D-07 exactly.</done>
</task>

<task type="auto">
  <name>Task 3: Create content seeds, data files, public assets, and verify build</name>
  <files>src/content/articles/welcome.md, src/content/projects/.gitkeep, src/content/creations/.gitkeep, src/content/microblog/.gitkeep, src/content/timeline/.gitkeep, src/content/friends/friends.json, src/content/anime/list.json, src/content/books/list.json, src/content/music/list.json, src/data/persona.yaml, src/data/notice.md, src/data/social.json, src/data/friends-health.json, src/styles/global.css, src/pages/index.astro, public/robots.txt, public/favicon.svg, public/og-default.svg, .env.example]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-PATTERNS.md §File Classification (per-file exact role + closest analog)
  - .planning/phases/01-foundation/01-RESEARCH.md §Code Example 5 lines 1012-1019 (robots.txt exact content)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pattern 3 lines 529-613 (global.css shape — light tokens only this plan; dark added 01-02)
  - .planning/phases/01-foundation/01-UI-SPEC.md §Color (light theme token values + accent reserved-for list)
  - .planning/phases/01-foundation/01-UI-SPEC.md §File Organization lines 474-525 (final directory tree)
  - .planning/phases/01-foundation/01-CONTEXT.md §Decisions D-14 (persona fields) and D-15 (notice bar)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 7 lines 776-787 (.env hygiene)
  - .planning/phases/01-foundation/01-RESEARCH.md §Anti-pattern 7 lines 666-668 (NO Tailwind dark: variant for tokens)
  </read_first>
  <action>
  Create the following files in this order:

  **Content collection seeds** (so `astro build` does not throw "no entries" warnings and the schemas are exercised):
  - `src/content/articles/welcome.md` — a single MDX sample post with frontmatter that exercises EVERY D-02 field: `title: "欢迎来到次元入口"`, `publishedAt: 2026-06-01`, `tags: ["notes"]`, `draft: false`, `description: "站点第一篇文章"`, `category: "notes"`, `sticky: true`, `toc: true`, plus a 1-paragraph body. This seed must validate against the articles schema.
  - `src/content/projects/.gitkeep` (empty)
  - `src/content/creations/.gitkeep` (empty)
  - `src/content/microblog/.gitkeep` (empty)
  - `src/content/timeline/.gitkeep` (empty)
  - `src/content/friends/friends.json` — `{"items": []}` (the `file()` loader requires the file exists at build time)
  - `src/content/anime/list.json` — `{"items": []}`
  - `src/content/books/list.json` — `{"items": []}`
  - `src/content/music/list.json` — `{"items": []}`

  **Data files** (D-14, D-15, D-03, footer social):
  - `src/data/persona.yaml` — populate with placeholder values for every D-14 field: `avatar: /avatars/persona.png` (relative to public/), `name: { zh: "merlinalex", romaji: "merlinalex" }`, `mbti: "INFJ"`, `zodiac: "双鱼座"`, `blood: "O"`, `tagline: "在代码与次元之间来回穿梭的小小人"`, `bio: "一个喜欢动画 / 写代码 / 画小画的家伙。"`, `skills: [{ name: "TypeScript", level: 85 }, { name: "Astro", level: 75 }, { name: "插画", level: 60 }, { name: "日语", level: 45 }]`, `favorites: { anime: [{ title: "孤独摇滚", year: 2022 }, { title: "摇曳露营", year: 2018 }], characters: [{ name: "后藤一里", source: "孤独摇滚" }, { name: "各务原抚子", source: "摇曳露营" }] }`, `qa: [{ q: "你叫什么？", a: "merlinalex。" }, { q: "你住哪里？", a: "在云端。" }]`.
  - `src/data/notice.md` — empty (just a YAML frontmatter `id: welcome-2026` then nothing); UI-SPEC line 542 makes the frontmatter `id` optional in Phase 1 but include it so plan 01-02's NoticeBar has a real id to dismiss on.
  - `src/data/social.json` — `{"links": [{"name": "GitHub", "url": "https://github.com/merlinalex"}, {"name": "Email", "url": "mailto:hi@merlinalex.me"}]}` (footer consumer per UI-SPEC line 265)
  - `src/data/friends-health.json` — `{}` (D-03 consumer-only in Phase 1)

  **Styles** — `src/styles/global.css` (light theme only this plan; dark + atmo gate + reduced-motion land in plan 01-02): the file starts with `@import "tailwindcss";` then declares `:root[data-theme="light"] { --color-bg: #FFF8FA; --color-bg-elevated: #FFFFFF; --color-surface: #FFE4ED; --color-surface-muted: #FFF0F4; --color-border: #F5D4DE; --color-fg: #2D1B2D; --color-fg-muted: #7A5D6F; --color-accent: #FF6B9D; --color-accent-hover: #E85A8B; --color-accent-subtle: #FFC8DD; --color-destructive: #E63946; --color-destructive-hover: #C1303B; --color-focus: #FF6B9D; --duration-fast: 150ms; --duration-base: 250ms; --duration-slow: 400ms; --easing-default: cubic-bezier(0.4, 0, 0.2, 1); --easing-bounce: cubic-bezier(0.34, 1.56, 0.64, 1); }` then base body styles referencing those tokens (`html { scroll-behavior: smooth; } body { background: var(--color-bg); color: var(--color-fg); font-family: "Zen Maru Gothic", "Noto Sans SC", "PingFang SC", "Hiragino Sans", system-ui, -apple-system, sans-serif; } :focus-visible { outline: 2px solid var(--color-focus); outline-offset: 2px; }`). Per Anti-pattern 7, DO NOT use Tailwind `dark:` variant for tokens — only the `:root[data-theme="..."]` selector drives the cascade. Per CONTEXT D-10, the reduced-motion rule is added in plan 01-02 (this plan leaves it out so the file is minimal).

  **Pages** — `src/pages/index.astro` (Hello-World placeholder for the Walking Skeleton): minimal frontmatter (`---` block) that imports `../styles/global.css`; a `<main>` element with a `<h1>` containing `"merlinalex.me — 次元入口"` and a `<p>` containing `"在二次元与代码之间来回穿梭的小站"` plus a friendly note `"（正式内容正在筹备中 ✨）"`. This is NOT the real Home — plan 01-03 replaces it. The point is that the dev server has a page to render right now.

  **Public assets**:
  - `public/robots.txt` — `User-agent: *`, `Allow: /`, blank line, `Sitemap: https://merlinalex.me/sitemap-index.xml` (per RESEARCH §Code Example 5)
  - `public/favicon.svg` — 200×200 SVG, sakura-pink (#FFC8DD) filled circle with a centered "M" in #FF6B9D (vector SVG markup; do not embed a PNG)
  - `public/og-default.svg` — 1200×630 SVG, vertical gradient from #FFE4ED to #FFC8DD with a centered "merlinalex.me" in 64px #FF6B9D. PNG conversion is Phase 6 polish (UI-SPEC line 514: SVG placeholder is acceptable).
  - `.env.example` — single line `TWIKOO_ENV_ID=` (per D-11 / Pitfall 7 — deployed empty in Phase 1; the `.env` is gitignored, the `.env.example` is the only env file in git)

  **Final verification**:
  - Run `pnpm build`. It MUST exit 0. If it fails, the most likely causes are: (a) `src/content.config.ts` is at the wrong path (Astro 6 throws `LegacyContentConfigError` if at `src/content/config.ts`), (b) the placeholder JSON files are missing, (c) `welcome.md` frontmatter fails Zod validation.
  - Run `pnpm dev` in the background, then `curl -s http://localhost:4321/ | grep -c "次元入口"` — must return at least 1. Stop the dev server.
  - Verify `dist/index.html` exists after the build.
  - Commit with message `chore(phase-01): scaffold Astro 6 + Tailwind v4 + 9 content collection schemas`.

  Do NOT install `@astrojs/tailwind` (Pitfall 3 — Tailwind 3 legacy). Do NOT add `tailwind()` to the `integrations` array. Do NOT create `src/content/config.ts` (Pitfall 4 — wrong Astro 6 path). Do NOT use Tailwind `dark:` variant for color tokens (Anti-pattern 7). Do NOT commit a populated `.env` file (Pitfall 7 — only `.env.example` is committed).
  </action>
  <verify>
  <automated>pnpm -s build 2>&1 | tail -20 && test -f dist/index.html && grep -c "@tailwindcss/tailwind\\|@astrojs/tailwind" package.json astro.config.mjs 2>/dev/null | (read n; test "$n" = "0" && echo "OK: no legacy tailwind integration")</automated>
  </verify>
  <acceptance_criteria>
  - `src/content/articles/welcome.md` exists with frontmatter including `title`, `publishedAt`, `tags: ["notes"]`, `draft: false`, `sticky: true`, `toc: true`
  - `src/content/friends/friends.json` exists and is `{"items": []}` (valid JSON)
  - `src/content/{anime,books,music}/list.json` each exist and are `{"items": []}` (3 files)
  - Empty `.gitkeep` files exist in `src/content/{projects,creations,microblog,timeline}/` (4 files)
  - `src/data/persona.yaml` exists and contains every D-14 field: `name.zh`, `name.romaji`, `mbti`, `zodiac`, `blood`, `tagline`, `bio`, `skills[]`, `favorites.anime[]`, `favorites.characters[]`, `qa[]`
  - `src/data/notice.md` exists and contains `id: welcome-2026` frontmatter
  - `src/data/social.json` exists and contains a `links` array with at least 1 GitHub link
  - `src/data/friends-health.json` exists and is `{}`
  - `src/styles/global.css` exists, contains `@import "tailwindcss";`, contains `:root[data-theme="light"]`, contains all 12 `--color-*` tokens per UI-SPEC lines 81-95, and contains `body { background: var(--color-bg); color: var(--color-fg);`
  - `src/styles/global.css` does NOT contain `dark:` variant declarations (Anti-pattern 7)
  - `src/styles/global.css` does NOT contain `@media (prefers-reduced-motion` (added in plan 01-02)
  - `src/pages/index.astro` exists, contains a `<main>` element, contains the Chinese text `"次元入口"`, and contains the placeholder note `"（正式内容正在筹备中 ✨）"`
  - `public/robots.txt` exists, contains `User-agent: *`, `Allow: /`, and `Sitemap: https://merlinalex.me/sitemap-index.xml`
  - `public/favicon.svg` exists, is valid SVG, references `#FFC8DD` or `#FF6B9D` colors
  - `public/og-default.svg` exists, is valid SVG with 1200×630 viewBox
  - `.env.example` exists and contains `TWIKOO_ENV_ID=` (empty value)
  - `.gitignore` exists and contains `.env`, `.env.local`, `.env.*.local`, `node_modules`, `dist`, `.astro`, `.DS_Store`
  - `pnpm build` exits 0 (verify: `pnpm build; echo $?` returns `0`)
  - `dist/index.html` exists after build
  - `pnpm dev` starts on a port, `curl http://localhost:4321/` returns HTML containing `次元入口`
  - `package.json` does NOT contain `@astrojs/tailwind` (Pitfall 3)
  - `astro.config.mjs` does NOT contain `integrations: [tailwind()]` (Pitfall 3)
  - `src/content.config.ts` exists, `src/content/config.ts` does NOT exist (Pitfall 4)
  </acceptance_criteria>
  <done>Walking Skeleton baseline is up: the dev server renders a styled Chinese Hello-World placeholder, `pnpm build` exits 0, all 9 content collections validate against the schemas, all 4 D-14 persona fields are populated with placeholders, and the `dist/` output is ready for plan 01-02 to extend. The scaffold commit is made.</done>
</task>

</tasks>

<security>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local `node_modules` | Third-party packages installed during scaffold (verified list per RESEARCH §Package Legitimacy Audit lines 162-186) |
| Build pipeline → `dist/` | Static HTML/CSS/JS emitted to public directory; no server, no user input in Phase 1 |
| Repository → Git history | `.env` must NEVER be committed; only `.env.example` with empty `TWIKOO_ENV_ID=` is committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | Per RESEARCH §Package Legitimacy Audit, all packages are first-party Astro integrations (`@astrojs/*`) or well-known (`@tailwindcss/vite`, `tailwindcss`, `zod`) or font-only (`@fontsource/*`); `@fontsource/zen-maru-gothic`, `@fontsource/jetbrains-mono`, `astro-icon`, `vitest`, `@playwright/test` marked `[ASSUMED]` but are long-established multi-year packages with no runtime risk; slopcheck not run this phase but no `[SLOP]` or `[SUS]` candidates flagged |
| T-01-01 | Information Disclosure | `.env` file | mitigate | `.gitignore` includes `.env`, `.env.local`, `.env.*.local`; only `.env.example` (with empty `TWIKOO_ENV_ID=`) is committed per CONTEXT D-11 / RESEARCH Pitfall 7 lines 776-787 |
| T-01-02 | Tampering | build pipeline | accept | Phase 1 has no user input, no forms, no POSTs, no client-side fetches; the only build-time inputs are the static files in `src/content/`, `src/data/`, and `public/` — all of which are author-controlled. No runtime attack surface. |
| T-01-03 | Repudiation | 404 status | mitigate (deferred to 01-03) | Plan 01-02/01-03 will set `Astro.response.status = 404` in `src/pages/404.astro` per D-16 / RESEARCH Pitfall 6 |
| T-01-04 | Information Disclosure | source maps in production | accept | Astro 6's default build emits no source maps in `dist/`; verified by inspecting build output |
</security>

<verification>
[Overall Phase 1 plan 01-01 checks]
- `pnpm build` exits 0 with no errors and no warnings about deprecated APIs
- `pnpm dev` serves a styled Chinese Hello-World page on `http://localhost:4321/`
- `dist/index.html` contains `次元入口` and `<title>...merlinalex.me` references
- `src/content.config.ts` is at the Astro 6 path (not the v5 path)
- 9 collections defined in `src/content.config.ts`; all 9 seed files (markdown or JSON) exist and validate
- `package.json` has no `@astrojs/tailwind` reference anywhere
- `astro.config.mjs` has `vite.plugins: [tailwindcss()]` (not `integrations: [tailwind()]`)
- `dist/sitemap-index.xml` exists (the @astrojs/sitemap integration ran during build)
- `dist/robots.txt` exists and matches the source `public/robots.txt`
- `pnpm-lock.yaml` is committed to the repo
- Walking Skeleton baseline: real Hello-World page renders on dev server, real build produces real `dist/`
</verification>

<success_criteria>
[Measurable completion of plan 01-01]
- A greenfield Astro 6.4.2 + Tailwind v4.3.0 + TypeScript strict + pnpm + Node 22 LTS project lives at `/Users/huangjingping/i/merlinalex.me/` and meets every acceptance criterion in the 3 tasks above
- A `pnpm dev` invocation serves a styled Chinese Hello-World on `localhost:4321` with no console errors
- A `pnpm build` invocation exits 0 and produces `dist/index.html` + `dist/sitemap-index.xml` + `dist/robots.txt`
- All 9 Zod-validated content collections (6 detailed per D-01 + 3 placeholder) live in `src/content.config.ts` at the Astro 6 path
- All 10 D-14 persona fields are populated in `src/data/persona.yaml` with sensible placeholder values
- The Walking Skeleton is in place: subsequent plans (01-02 BaseLayout + theme + a11y; 01-03 Home + About + 404 + deploy) can build on this baseline without re-doing the scaffold
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
