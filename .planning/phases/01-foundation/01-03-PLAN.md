---
phase: 01-foundation
plan: 03
type: execute
wave: 1
depends_on:
  - 01-02
files_modified:
  - src/components/home/Hero.astro
  - src/components/home/LatestArticles.astro
  - src/components/home/LatestMicroblog.astro
  - src/components/home/Hitokoto.astro
  - src/components/home/SiteStats.astro
  - src/components/about/PersonaCard.astro
  - src/components/about/PersonaStats.astro
  - src/components/about/SkillBars.astro
  - src/components/about/PersonaFavorites.astro
  - src/components/about/PersonaQA.astro
  - src/pages/index.astro
  - src/pages/about.astro
  - src/pages/404.astro
  - src/data/persona.yaml
  - public/_redirects
  - .planning/STATE.md
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - INFRA-03
  - INFRA-06
must_haves:
  truths:
    - "Visitor can open `/` and see a styled Home (PAGE-01) with: notice bar, hero (name + tagline + primary CTA `进入次元` → `/about`), Hitokoto placeholder `「 一言 」`, SiteStats zero-state row (运行时间 / 文章数 / 总字数), LatestArticles slot (top 3 from `articles` collection excluding drafts, or empty state `还没有文章`), LatestMicroblog slot (top 5 from `microblog` collection, or empty state `暂无说说`) — D-15, PAGE-01"
    - "Visitor can navigate to `/about` and see the persona card (PAGE-02) with: 200px circle avatar + name (zh + romaji in parentheses) + tagline + 3 stat chips (MBTI / zodiac / blood-type, `border: 1px solid var(--color-accent)`, `border-radius: full`) + 4 skill bars (label + bar + percent) + 2-column favorites (最喜欢的动画 + 最喜欢的角色) + 2-5 Q&A pairs — D-14, PAGE-02"
    - "Persona data is loaded from `src/data/persona.yaml` at build time via `import persona from '../data/persona.yaml'` (Astro 6 supports YAML imports natively, or via `@rollup/plugin-yaml` if needed); missing fields in the YAML cause their corresponding section in the persona card to be omitted entirely (no `coming soon` placeholders inside the card) — D-14, UI-SPEC line 334"
    - "Visitor hitting any non-existent URL sees the custom 404 page (kawaii mascot + `咦？这里什么都没有…` heading + `迷路了？要不然回到首页吧` body + `回到首页` CTA → `/`); the HTTP response is `HTTP/2 404` (verified via `curl -I`) — D-16, INFRA-06, PITFALLS P-21"
    - "`src/pages/404.astro` frontmatter contains `Astro.response.status = 404;` and `Astro.response.statusText = 'Not Found';` (NOT a meta refresh redirect; NOT a 200 with 404 UI) — PITFALLS P-21"
    - "`dist/sitemap-index.xml` and `dist/sitemap-0.xml` are generated by `@astrojs/sitemap` integration at build time; `public/robots.txt` references the sitemap URL — SEO-01"
    - "`pnpm build` exits 0; `dist/index.html` contains the pre-paint script before any stylesheet (invariant from plan 01-02 preserved); `dist/404.html` exists; `dist/sitemap-index.xml` exists"
    - "Cloudflare Pages is configured to: build with `pnpm build`, output to `dist/`, env vars `NODE_VERSION=22` and `NODE_OPTIONS=--max-old-space-size=4096`; custom domain `merlinalex.me` is wired with `www.merlinalex.me` 301-redirected to apex (via dashboard toggle + `public/_redirects` fallback) — D-11, D-12, INFRA-03"
    - "Theme switcher, intensity badge, reduced-motion behavior from plan 01-02 all work on Home, About, and 404 pages (no FOUC, no console errors)"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "PAGE-01 Home page with all 6 section slots in the locked order (NoticeBar → Hero → Hitokoto → SiteStats → LatestArticles → LatestMicroblog)"
      contains: "getCollection('articles'"
    - path: "src/pages/about.astro"
      provides: "PAGE-02 About page composing PersonaCard from src/data/persona.yaml"
      contains: "import persona from"
    - path: "src/pages/404.astro"
      provides: "Custom 404 with Astro.response.status = 404 frontmatter and NotFound component"
      contains: "Astro.response.status = 404"
    - path: "src/components/home/Hero.astro"
      provides: "Display name (from persona.yaml name.zh) + tagline + `进入次元` CTA → /about"
      contains: "进入次元"
    - path: "src/components/home/LatestArticles.astro"
      provides: "Top 3 articles or empty state `还没有文章`"
      contains: "还没有文章"
    - path: "src/components/home/LatestMicroblog.astro"
      provides: "Top 5 microblog or empty state `暂无说说`"
      contains: "暂无说说"
    - path: "src/components/home/SiteStats.astro"
      provides: "3-column zero-state row with `运行时间` / `文章数` / `总字数` placeholders"
      contains: "运行时间"
    - path: "src/components/about/PersonaCard.astro"
      provides: "Container composing all 4 sub-components from persona prop"
    - path: "src/components/about/PersonaStats.astro"
      provides: "3 chips (MBTI / zodiac / blood) with accent border, radius-full"
    - path: "src/components/about/SkillBars.astro"
      provides: "Skill list rendering bars with --color-accent fill on --color-accent-subtle track"
    - path: "src/data/persona.yaml"
      provides: "All D-14 fields populated with real placeholder values; serves both Hero (Home) and PersonaCard (About)"
    - path: "public/_redirects"
      provides: "Fallback www → apex 301 redirect (in addition to CF dashboard toggle)"
      contains: "301"
    - path: "dist/404.html"
      provides: "Built 404 page served by CF Pages for any unmatched URL"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/content/articles/*"
      via: "getCollection('articles', filter callback) + sort by publishedAt desc + slice 3"
      pattern: "getCollection\\('articles'"
    - from: "src/pages/index.astro"
      to: "src/content/microblog/*"
      via: "getCollection('microblog') + sort by publishedAt desc + slice 5"
      pattern: "getCollection\\('microblog'"
    - from: "src/pages/about.astro"
      to: "src/data/persona.yaml"
      via: "import persona from '../data/persona.yaml' (Astro 6 native YAML)"
      pattern: "import persona from"
    - from: "src/pages/404.astro"
      to: "Astro.response"
      via: "Astro.response.status = 404 in frontmatter"
      pattern: "Astro\\.response\\.status = 404"
    - from: "src/components/home/Hero.astro"
      to: "src/data/persona.yaml"
      via: "Passes `persona` prop down from parent or imports directly"
      pattern: "persona\\.name\\.zh"
    - from: "public/_redirects"
      to: "Cloudflare Pages"
      via: "Static redirect rules served before any 404 lookup"
      pattern: "https://www\\.merlinalex\\.me"
---

<objective>
Build the three real Phase 1 pages (Home / About / 404) on top of the `BaseLayout` from plan 01-02. Fill `src/data/persona.yaml` with concrete placeholder values, write the 10 page-level components (5 Home + 5 About), wire the Cloudflare Pages deployment configuration (env vars, custom domain, `www` → apex 301 redirect via dashboard toggle + `public/_redirects` fallback), and document the owner-side deploy steps. After this plan, every Phase 1 success criterion from the ROADMAP is met: a visitor can land on `https://merlinalex.me/`, see the styled Home, navigate to `/about`, toggle the theme with no FOUC, and any non-existent URL returns `HTTP 404` with the kawaii 404 UI.

Purpose: Plan 01-03 closes the Walking Skeleton loop — the scaffold + theme system from 01-01/01-02 are no longer a Hello-World; they are the real product surface. Every later phase (articles, works, microblog, atmosphere) builds on these pages without touching the foundation.

Output: A greenfield production-ready Astro 6 site ready for the first push to Cloudflare Pages, with the owner-side deploy steps documented in the commit body.

<user_story>
**As a** visitor, **I want to** land on a styled Home page with a kawaii hero, navigate to an About page with a persona card, and have the site respect my theme + reduced-motion preferences across every page, **so that** I can explore a personal site that feels alive and uniquely mine from the very first visit.
</user_story>
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/huangjingping/i/merlinalex.me/.planning/STATE.md
@/Users/huangjingping/i/merlinalex.me/.planning/ROADMAP.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-RESEARCH.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-PATTERNS.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-UI-SPEC.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-01-SUMMARY.md
@/Users/huangjingping/i/merlinalex.me/.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Home components and `src/pages/index.astro`</name>
  <files>src/components/home/Hero.astro, src/components/home/LatestArticles.astro, src/components/home/LatestMicroblog.astro, src/components/home/Hitokoto.astro, src/components/home/SiteStats.astro, src/pages/index.astro]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-UI-SPEC.md §Home (lines 291-315) — locked section order + empty-state contract
  - .planning/phases/01-foundation/01-UI-SPEC.md §Copywriting (lines 380-417) — exact Chinese copy for every section
  - .planning/phases/01-foundation/01-UI-SPEC.md §Component Inventory (Home components, lines 270-274)
  - .planning/phases/01-foundation/01-RESEARCH.md §Code Example 4 lines 977-1008 (verified getCollection + sort + slice pattern)
  - .planning/phases/01-foundation/01-PATTERNS.md §Pattern E (Astro 6 content API: getCollection, getEntry, entry.id)
  - .planning/phases/01-foundation/01-CONTEXT.md §Decisions D-15 (home content slots)
  - /Users/huangjingping/i/merlinalex.me/src/data/persona.yaml (D-14 placeholder values from plan 01-01)
  - /Users/huangjingping/i/merlinalex.me/src/data/notice.md (for NoticeBar consumption)
  - /Users/huangjingping/i/merlinalex.me/src/components/core/NoticeBar.astro (from plan 01-02)
  - /Users/huangjingping/i/merlinalex.me/src/layouts/BaseLayout.astro (from plan 01-02)
  </read_first>
  <action>
  Create 5 Home components in `src/components/home/`, then replace `src/pages/index.astro`.

  **A. `Hero.astro`** — receives `persona` prop OR imports `import persona from '../../data/persona.yaml'` directly (single source of truth). Render a `<section class="hero">` containing `<h1 class="hero-name">{persona.name.zh}</h1>` (32px per UI-SPEC Display size), `<p class="hero-tagline">{persona.tagline}</p>` (16px italic muted), and `<a class="cta" href="/about">进入次元</a>` (the locked primary CTA copy per UI-SPEC line 386; the link target is `/about` per D-15 — not `/`, since the page IS Home). Add a `<style>` block that centers the section, applies `padding: var(--space-2xl) 0 var(--space-xl)`, sets the CTA to `background: var(--color-accent); color: white; padding: 12px 32px; border-radius: var(--radius-md); display: inline-block; min-height: 56px;` (56px touch target per UI-SPEC spacing exception), and adds `transition: background-color var(--duration-fast) var(--easing-default);` (overridden by reduce-motion rule).

  **B. `LatestArticles.astro`** — receives `articles` prop (an array of collection entries). Render `<section class="latest-articles">` with `<h2>最新文章</h2>` (24px heading). If `articles.length === 0`, render the empty-state card: `<div class="empty-state">` containing `<h3>还没有文章</h3>` + `<p>撰写第一篇之后会出现在这里 ✨</p>` + `<a href="/articles">撰写第一篇</a>` (the link is a 404 in Phase 1; that's fine — Phase 2 makes it real). Otherwise render a `<ul class="article-grid">` with 3 `<li>` items, each containing `<a href={'/articles/' + article.id}>` (Astro 6 uses `entry.id` for slug-based routing per RESEARCH State of the Art), an `<h3>{article.data.title}</h3>`, an optional `<p class="excerpt">{article.data.description}</p>`, and a `<time datetime={article.data.publishedAt.toISOString()}>{article.data.publishedAt.toLocaleDateString('zh-CN')}</time>`. The link to `/articles/<id>` will 404 in Phase 1 since the dynamic route doesn't exist yet — that's expected; the slot renders the list correctly so Phase 2 just adds the `[id].astro` route.

  **C. `LatestMicroblog.astro`** — receives `posts` prop. Render `<section class="latest-microblog">` with `<h2>最近的碎碎念</h2>`. If `posts.length === 0`, render `<div class="empty-state mascot-peek">` containing `<h3>暂无说说</h3>` + `<p>（等我有话想说的时候再来看看吧）</p>`. Otherwise render `<ul class="microblog-list">` with up to 5 `<li>` items, each containing `<time>{post.data.publishedAt.toLocaleDateString('zh-CN')}</time>` and a `<p>{post.data.content}</p>` (truncated to 100 chars + `…` if longer). Empty state visual: small mascot peeking from bottom-right corner (use a CSS `::after` pseudo-element with a gradient circle for the placeholder per UI-SPEC line 312).

  **D. `Hitokoto.astro`** — placeholder per UI-SPEC line 301. Render `<aside class="hitokoto" aria-label="一言">` containing the literal text `「 一言 」` (centered, italic, 16px muted). Phase 4 replaces this with a `client:visible` fetch from `hitokoto.cn`; the placeholder copy is locked by UI-SPEC line 390.

  **E. `SiteStats.astro`** — receives `articleCount` and `totalWords` props. Render `<section class="site-stats">` with a 3-column row: each cell is `<div class="stat">` containing `<div class="stat-label">运行时间 / 文章数 / 总字数</div>` and `<div class="stat-value">即将上线 / <span>{articleCount}</span> 篇 / <span>{totalWords}</span> 字</div>`. Per UI-SPEC line 391-393, Phase 1 shows zero state — the runtime shows `即将上线` (Phase 4 will fill it with `Math.floor((Date.now() - SITE_LAUNCH_DATE) / 86400000)} 天`), article count shows the actual `getCollection('articles', ({ data }) => !data.draft).length`, total words shows `0 字` in Phase 1 (Phase 4 computes from rendered article bodies).

  **F. `src/pages/index.astro`** — REPLACE the Hello-World placeholder from plan 01-01. Frontmatter: import `getCollection` from `astro:content`, import `BaseLayout` from `../layouts/BaseLayout.astro`, import `NoticeBar` from `../components/core/NoticeBar.astro`, import `Hero` from `../components/home/Hero.astro`, import `Hitokoto` from `../components/home/Hitokoto.astro`, import `SiteStats` from `../components/home/SiteStats.astro`, import `LatestArticles` from `../components/home/LatestArticles.astro`, import `LatestMicroblog` from `../components/home/LatestMicroblog.astro`, import `persona` from `../data/persona.yaml`. Build-time data fetches per RESEARCH §Code Example 4: `const allArticles = await getCollection('articles', ({ data }) => !data.draft); const latestArticles = allArticles.sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime()).slice(0, 3);` and `const allMicroblog = await getCollection('microblog'); const latestMicroblog = allMicroblog.sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime()).slice(0, 5);`. The `totalWords` prop passes `0` in Phase 1 (Phase 4 computes from rendered article bodies). Template: `<BaseLayout title="首页" description="merlinalex.me — 二次元可爱风个人站" type="website">` containing the 6 sections in this EXACT order (locked by UI-SPEC lines 295-306): `<NoticeBar />`, `<Hero />`, `<Hitokoto />`, `<SiteStats articleCount={allArticles.length} totalWords={0} />`, `<LatestArticles articles={latestArticles} />`, `<LatestMicroblog posts={latestMicroblog} />`. The empty-state branches are handled inside each child component based on the array length.

  Critical: the page uses `getCollection` with the filter callback for drafts (NEVER `getCollection('articles')` then `.filter(...)` — the filter-callback form is the Astro 6 idiom and skips drafts at the loader level). Use `entry.id` (Astro 6), NEVER `entry.slug` (removed). Do NOT sort microblog by `archived` — the auto-archive >180d flag is derived in the render layer, not stored, per D-05.
  </action>
  <verify>
  <automated>test -f src/pages/index.astro && test -f src/components/home/Hero.astro && test -f src/components/home/LatestArticles.astro && grep -c "进入次元\\|还没有文章\\|暂无说说\\|运行时间" src/components/home/*.astro | (read n; test "$n" -ge 4 && echo "OK: Home section copy present")</automated>
  </verify>
  <acceptance_criteria>
  - All 5 Home components exist at `src/components/home/{Hero,LatestArticles,LatestMicroblog,Hitokoto,SiteStats}.astro`
  - `Hero.astro` renders the display name (32px), tagline, and a CTA link `href="/about"` containing the text `进入次元`
  - `LatestArticles.astro` accepts an `articles` prop and renders the empty-state with the text `还没有文章` when the array is empty
  - `LatestMicroblog.astro` accepts a `posts` prop and renders the empty-state with the text `暂无说说` when the array is empty
  - `Hitokoto.astro` contains the literal text `「 一言 」` (the locked Phase 1 placeholder)
  - `SiteStats.astro` renders a 3-column row with labels `运行时间` / `文章数` / `总字数`
  - `src/pages/index.astro` REPLACES the Hello-World placeholder (no `次元入口` text from plan 01-01 should remain; that text moves to plan 01-01's git history)
  - `index.astro` imports `getCollection` from `astro:content`
  - `index.astro` calls `getCollection('articles', ({ data }) => !data.draft)` with the filter-callback form
  - `index.astro` calls `getCollection('microblog')` and sorts by `publishedAt desc`
  - `index.astro` slices the article list to 3 and the microblog list to 5
  - `index.astro` uses `<BaseLayout title="首页">` with the locked Chinese title
  - `index.astro` renders the 6 sections in the locked order: NoticeBar → Hero → Hitokoto → SiteStats → LatestArticles → LatestMicroblog
  - `index.astro` imports `persona` from `../data/persona.yaml` and passes it (or its name/tagline) to the Hero
  - The page does NOT use `entry.slug` (Astro 5 — removed) — verified by grep returning 0
  - The page does NOT call `getEntryBySlug` (Astro 5 — removed) — verified by grep returning 0
  </acceptance_criteria>
  <done>Home (PAGE-01) is live with all 6 section slots in the locked order, the empty-state branches are handled, `getCollection` is called with the correct Astro 6 filter-callback form, and the page wraps in `BaseLayout` so the theme/atmo gate from plan 01-02 applies. The Hello-World placeholder is replaced.</done>
</task>

<task type="auto">
  <name>Task 2: Create About components, `src/pages/about.astro`, and `src/pages/404.astro`</name>
  <files>src/components/about/PersonaCard.astro, src/components/about/PersonaStats.astro, src/components/about/SkillBars.astro, src/components/about/PersonaFavorites.astro, src/components/about/PersonaQA.astro, src/pages/about.astro, src/pages/404.astro, src/data/persona.yaml]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-UI-SPEC.md §About (lines 317-336) — locked layout + section contract
  - .planning/phases/01-foundation/01-UI-SPEC.md §404 stub (lines 338-352) — kawaii placeholder + HTTP 404 contract
  - .planning/phases/01-foundation/01-UI-SPEC.md §Component Inventory (About components, lines 275-279)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pattern 4 lines 636-656 (verified 404 pattern with Astro.response.status = 404)
  - .planning/phases/01-foundation/01-PATTERNS.md §File Classification (about.astro YAML load + 404.astro invariants)
  - .planning/phases/01-foundation/01-CONTEXT.md §Decisions D-14 (persona card content) and D-16 (404 stub)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 6 lines 760-774 (404 status code)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 8 lines 788-800 (404 vs CF Pages default)
  - /Users/huangjingping/i/merlinalex.me/src/data/persona.yaml (current placeholder values from plan 01-01)
  - /Users/huangjingping/i/merlinalex.me/src/components/core/NotFound.astro (from plan 01-02)
  - /Users/huangjingping/i/merlinalex.me/src/layouts/BaseLayout.astro (from plan 01-02)
  </read_first>
  <action>
  Create 5 About components + 2 pages, and update `src/data/persona.yaml`.

  **A. `src/components/about/PersonaCard.astro`** — receives `persona` prop. Render `<article class="persona-card">` (the wrapper per UI-SPEC line 322) with these conditional sections, each rendered only if the corresponding field exists in the YAML (per UI-SPEC line 334, "missing persona fields → section omitted entirely"):
  - `<header class="persona-header">` — `<img class="persona-avatar" src={persona.avatar} alt={persona.name.zh + '的头像'} width="200" height="200" />` (200px circle per UI-SPEC line 323), `<h1>{persona.name.zh}</h1>`, `<p class="persona-romaji">({persona.name.romaji})</p>`, `<p class="persona-bio">{persona.tagline}</p>` (italic).
  - `<PersonaStats mbti={persona.mbti} zodiac={persona.zodiac} blood={persona.blood} />` (always render if any of the 3 fields present)
  - `<SkillBars skills={persona.skills} />` (if `persona.skills?.length > 0`)
  - `<PersonaFavorites anime={persona.favorites?.anime} characters={persona.favorites?.characters} />` (if any favorites present)
  - `<PersonaQA qa={persona.qa} />` (if `persona.qa?.length > 0`)

  **B. `src/components/about/PersonaStats.astro`** — receives `mbti`, `zodiac`, `blood` props. Render `<div class="persona-stats">` with 3 chips, each a `<span class="stat-chip">` with `border: 1px solid var(--color-accent); border-radius: 9999px; padding: 4px 12px;` per UI-SPEC line 326. Each chip renders `<span class="stat-label">MBTI / 星座 / 血型</span> <span class="stat-value">{mbti/zodiac/blood}</span>` and is wrapped in `aria-label="属性：{value}"` for a11y. Conditionally render each chip only if the value is present.

  **C. `src/components/about/SkillBars.astro`** — receives `skills` prop (array of `{ name, level }`). Render `<div class="skill-bars">` with `<h2>技能</h2>` (locked heading per UI-SPEC line 399) then a `<ul>` of `<li>` items, each containing `<div class="skill-row">` with `<span class="skill-name">{skill.name}</span>`, `<div class="skill-track"><div class="skill-fill" style={`width: ${skill.level}%`}></div></div>`, and `<span class="skill-pct">{skill.level}%</span>`. The bar fill uses `background: var(--color-accent)` on a track of `background: var(--color-accent-subtle); height: 8px; border-radius: 9999px;`.

  **D. `src/components/about/PersonaFavorites.astro`** — receives `anime` and `characters` props (each an array of `{ title/name, year/source }`). Render `<div class="persona-favorites">` with two `<section>` columns (stacks on mobile per UI-SPEC line 328-330 responsive contract). Left column heading `最喜欢的动画`, mapping to `<ul><li><span>{a.title}</span><span class="year">({a.year})</span></li></ul>`. Right column heading `最喜欢的角色`, mapping to `<ul><li><span>{c.name}</span><span class="source">《{c.source}》</span></li></ul>`. Conditionally render each column only if its array has items.

  **E. `src/components/about/PersonaQA.astro`** — receives `qa` prop (array of `{ q, a }`). Render `<div class="persona-qa">` with `<h2>关于我</h2>` (locked heading per UI-SPEC line 402) and a `<dl>` of Q&A pairs: each `<div class="qa-item">` containing `<dt class="qa-q">{item.q}</dt>` and `<dd class="qa-a">{item.a}</dd>`. The question uses `--color-fg` semibold; the answer uses `--color-fg` regular; 2xl vertical rhythm between pairs per UI-SPEC line 331.

  **F. `src/pages/about.astro`** — frontmatter: import `BaseLayout` from `../layouts/BaseLayout.astro`, import `PersonaCard` from `../components/about/PersonaCard.astro`, import `persona from '../data/persona.yaml'` (Astro 6 supports YAML imports natively; if a build error indicates the import is unresolved, install `@rollup/plugin-yaml` via `pnpm add -D @rollup/plugin-yaml` and register it in `astro.config.mjs` `vite.plugins` — see EXTERNAL REFERENCE in PATTERNS.md). Template: `<BaseLayout title="关于" description={persona.tagline || 'merlinalex.me — 关于我'}>` containing `<PersonaCard persona={persona} />`. Set `lang="zh-CN"` via BaseLayout default.

  **G. `src/pages/404.astro`** — frontmatter: import `BaseLayout` from `../layouts/BaseLayout.astro`, import `NotFound` from `../components/core/NotFound.astro`. CRITICAL — set `Astro.response.status = 404;` AND `Astro.response.statusText = 'Not Found';` in the frontmatter (the two lines are non-negotiable per PITFALLS P-21 / RESEARCH Anti-pattern 8 / Anti-pattern 10). Template: `<BaseLayout title="404 · 迷路了？">` containing `<NotFound />`.

  **H. `src/data/persona.yaml`** — update with concrete placeholder values (not the bare-bones plan 01-01 values). Required fields (all from D-14): `avatar: /avatars/persona.png` (path only; the actual file ships in Phase 6 polish; use a fallback via `<img onerror>` if needed or simply accept the broken image placeholder for Phase 1), `name: { zh: "merlinalex", romaji: "merlinalex" }`, `mbti: "INFJ"`, `zodiac: "双鱼座"`, `blood: "O"`, `tagline: "在代码与次元之间来回穿梭的小小人"`, `bio: "一个喜欢动画 / 写代码 / 画小画的家伙。"`, `skills: [{ name: "TypeScript", level: 85 }, { name: "Astro", level: 75 }, { name: "插画", level: 60 }, { name: "日语", level: 45 }, { name: "调咖啡", level: 70 }]` (5 skills per UI-SPEC line 327), `favorites: { anime: [{ title: "孤独摇滚", year: 2022 }, { title: "摇曳露营", year: 2018 }, { title: "莉可丽丝", year: 2022 }], characters: [{ name: "后藤一里", source: "孤独摇滚" }, { name: "各务原抚子", source: "摇曳露营" }, { name: "锦木千束", source: "莉可丽丝" }] }`, `qa: [{ q: "你叫什么名字？", a: "merlinalex。这是一个网名。" }, { q: "你是做什么的？", a: "写代码、画小画、偶尔写写文章。" }, { q: "你最喜欢的季节是？", a: "春天。樱花飘落的季节。" }]`. The owner can edit these values in the YAML directly without changing any component code (single source of truth per D-14).
  </action>
  <verify>
  <automated>test -f src/pages/about.astro && test -f src/pages/404.astro && grep -c "Astro.response.status = 404" src/pages/404.astro | (read n; test "$n" -ge 1 && echo "OK: 404 status set") && grep -c "import persona from" src/pages/about.astro | (read n; test "$n" -ge 1 && echo "OK: persona YAML imported")</automated>
  </verify>
  <acceptance_criteria>
  - All 5 About components exist at `src/components/about/{PersonaCard,PersonaStats,SkillBars,PersonaFavorites,PersonaQA}.astro`
  - `PersonaCard.astro` conditionally renders each section based on whether the corresponding persona field is present (no "coming soon" placeholders inside the card)
  - `PersonaStats.astro` renders 3 chips with `border-radius: 9999px` (pill shape) and `border: 1px solid var(--color-accent)`
  - `SkillBars.astro` renders the heading `技能` and a list of bars with `width: ${level}%` inline style
  - `PersonaFavorites.astro` renders 2 columns with headings `最喜欢的动画` and `最喜欢的角色`
  - `PersonaQA.astro` renders the heading `关于我` and a `<dl>` of Q&A pairs
  - `src/pages/about.astro` exists, imports `persona` from `../data/persona.yaml`, wraps in `<BaseLayout title="关于">`
  - `src/pages/404.astro` exists
  - `src/pages/404.astro` frontmatter contains the EXACT line `Astro.response.status = 404;` (NOT a meta refresh redirect; NOT a 200 with 404 UI)
  - `src/pages/404.astro` frontmatter also contains `Astro.response.statusText = 'Not Found';`
  - `src/pages/404.astro` uses `<NotFound />` from `../components/core/NotFound.astro`
  - `src/data/persona.yaml` is updated with all D-14 fields populated: `avatar`, `name.zh`, `name.romaji`, `mbti`, `zodiac`, `blood`, `tagline`, `bio`, `skills` (5 items), `favorites.anime` (3 items), `favorites.characters` (3 items), `qa` (3 items)
  - The persona YAML is valid (parses without error — verified by `pnpm build` exiting 0)
  </acceptance_criteria>
  <done>About (PAGE-02) and 404 (INFRA-06) pages are wired with the kawaii persona card and the 404 returning `HTTP 404` status. persona.yaml is populated with real placeholder values for all D-14 fields. The site is functionally complete on the static side; deploy config lands in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Cloudflare Pages deploy config + final verification + commit + STATE update</name>
  <files>public/_redirects, .planning/STATE.md, astro.config.mjs]<]minimax[>[</files>
  <read_first>
  - .planning/phases/01-foundation/01-CONTEXT.md §Decisions D-11 (Cloudflare Pages deploy) and D-12 (build hardening)
  - .planning/phases/01-foundation/01-RESEARCH.md §Code Example 6 (CF Pages env vars) + §Code Example 7 (custom domain + 301)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 5 lines 751-758 (build OOM)
  - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 8 lines 788-800 (404 vs CF Pages default)
  - .planning/phases/01-foundation/01-RESEARCH.md §Assumption A7 (CF dashboard 301 toggle fallback)
  - .planning/phases/01-foundation/01-UI-SPEC.md §File Organization (final tree)
  - .planning/phases/01-foundation/01-RESEARCH.md §Open Question 1 (CF Pages vs Workers)
  - /Users/huangjingping/i/merlinalex.me/astro.config.mjs (current state)
  - /Users/huangjingping/i/merlinalex.me/public/robots.txt (from plan 01-01)
  - /Users/huangjingping/i/merlinalex.me/.planning/STATE.md (current state — Phase 1 plan 0 of 3)
  </read_first>
  <action>
  Three deliverables for this task: a `_redirects` fallback file, final build verification, and the commit + STATE.md update.

  **A. `public/_redirects`** — fallback for the `www.merlinalex.me` → `merlinalex.me` 301 redirect, in case the CF Pages dashboard toggle is unavailable or removed. Per Cloudflare Pages `_redirects` syntax: one rule per line, `source destination status_code`. Content (2 lines):
  ```
  https://www.merlinalex.me/* https://merlinalex.me/:splat 301
  ```
  The trailing `:splat` is CF Pages syntax for "forward the matched path". Status `301` is a permanent redirect. The rule covers `https://www.merlinalex.me/` and any subpath. No other redirects in Phase 1.

  **B. Final build verification** — run `pnpm build` and assert every deliverable:
  1. `pnpm build` exits 0 (the production build succeeds with all 12 new components + 2 new pages + 1 redirect file)
  2. `test -f dist/index.html` (the Home page is built)
  3. `test -f dist/about/index.html` (the About page is built; the trailingSlash: 'never' in `astro.config.mjs` should mean `dist/about.html` is the file — adjust the test based on what Astro actually emits; whichever exists, verify it)
  4. `test -f dist/404.html` (the 404 page is built per Astro 6's convention from `src/pages/404.astro`)
  5. `test -f dist/sitemap-index.xml` and `test -f dist/sitemap-0.xml` (the @astrojs/sitemap integration ran)
  6. `test -f dist/robots.txt` (the static asset is copied)
  7. `grep -c "Astro.response.status = 404" dist/404.html` is 0 (because the frontmatter only runs at build time, not in the output HTML — the status is in the HTTP response, not the file content; this is correct behavior, not a failure). INSTEAD verify by starting `pnpm preview` and running `curl -I http://localhost:4321/nonexistent-path-12345` which should return `HTTP/1.1 404 Not Found`. (If `pnpm preview` is not available, use `pnpm dev` and check the same — the status is set at request time in dev too.)
  8. `pnpm dev` in background; `curl -I http://localhost:4321/nonexistent-path` → `HTTP/1.1 404 Not Found`; `curl -s http://localhost:4321/nonexistent-path | grep -c "咦？这里什么都没有…"` ≥ 1; kill dev server.
  9. `curl -s http://localhost:4321/ | grep -c "进入次元"` ≥ 1 (Home page has the hero CTA)
  10. `curl -s http://localhost:4321/about | grep -c "属性"` OR `grep -c "技能"` OR `grep -c "最喜欢的动画"` ≥ 1 (About page has the persona card)
  11. `curl -s http://localhost:4321/robots.txt | grep -c "Sitemap:"` ≥ 1
  12. `curl -s http://localhost:4321/sitemap-index.xml | grep -c "<sitemap"` ≥ 1 (valid sitemap XML)
  13. Inspect `dist/index.html` source order: `grep -n "<script is:inline" dist/index.html` line number must be LESS than `grep -n "<link\\|<style" dist/index.html` line number (the pre-paint script runs before any stylesheet — the FOUC-safe invariant from plan 01-02 must still hold in production)
  14. `grep -c "data-theme=\"" dist/index.html` ≥ 1 (the pre-paint script embedded an initial data-theme; the value will be `"light"` or `"dark"` depending on resolution, but the attribute must be present in the rendered HTML)

  If any check fails, fix the underlying issue before committing. Do NOT commit a broken site.

  **C. Document the owner-side Cloudflare Pages deploy steps in the commit body** (the actual dashboard interactions are not automatable from the CLI without CF API credentials; the owner does them). The commit body must include:
  ```
  ## Cloudflare Pages deploy (owner task)

  1. Connect Cloudflare account to the GitHub repo (or push to a new GitHub repo first)
  2. Create Pages project → Framework preset: Astro
  3. Build command: pnpm build
  4. Build output directory: dist
  5. Environment variables:
     - NODE_VERSION = 22
     - NODE_OPTIONS = --max-old-space-size=4096  (CONTEXT D-12 OOM guard)
  6. Custom domains:
     - Add merlinalex.me (apex) — CF auto-provisions SSL
     - Add www.merlinalex.me → set "Redirect to primary domain" toggle ON (apex)
     - The public/_redirects file is a fallback if the dashboard toggle is unavailable
  7. Push to main → first production build triggers
  8. Verify post-deploy:
     - curl -I https://merlinalex.me/ → HTTP/2 200
     - curl -I https://merlinalex.me/nonexistent → HTTP/2 404
     - curl https://merlinalex.me/sitemap-index.xml → valid XML
     - curl https://merlinalex.me/robots.txt → contains "Sitemap:"
     - Hard refresh https://merlinalex.me/ in browser; theme toggle works; no FOUC
  ```

  The commit message itself: `feat(phase-01): Home + About + 404 + Cloudflare Pages deploy`. Reference all 4 requirement IDs in the body: `PAGE-01, PAGE-02, INFRA-03, INFRA-06`.

  **D. Update `.planning/STATE.md`** — mark Phase 1 complete. Edit the `progress` block to: `total_plans: 3`, `completed_plans: 3`, `percent: 4` (1/6 phases * 100 ≈ 17% of the milestone; the existing percent field has been 0 so far — check whether the field tracks phase percent or plan percent; if plan percent, set to `3/3 = 100` for the phase; if phase percent, set to `~17`). Update `last_activity` to today's date. Update `last_updated` to today's ISO timestamp. Update the `By Phase` table to show `1. Foundation | 3/3 | - | -` and the `Recent Trend` to `Last 3 plans: 01-01, 01-02, 01-03 — sequential, all complete`. Add a `## Phase 1 Closeout` section summarizing what was shipped: scaffold + 9 Zod-validated content collections + FOUC-safe theme/atmo gate + reduced-motion baseline + Home + About + 404 + Cloudflare Pages deploy config. Note the deferred items (atmosphere, Twikoo, Bangumi, RSS, search, JSON-LD, 80% tests) per the SKELETON.md Out-of-Scope list. The next phase is Phase 2: Core Content.

  The plan's `output` step (create the SUMMARY) is the executor's job after the commit; do NOT create it in this task — the executor writes it.
  </action>
  <verify>
  <automated>pnpm -s build 2>&1 | tail -3 && test -f dist/index.html && test -f dist/404.html && test -f dist/sitemap-index.xml && test -f dist/sitemap-0.xml && test -f public/_redirects && grep -c "301" public/_redirects | (read n; test "$n" -ge 1 && echo "OK: www→apex redirect")</automated>
  </verify>
  <acceptance_criteria>
  - `pnpm build` exits 0 after all components + pages are created
  - `dist/index.html` exists (Home page built)
  - `dist/about.html` or `dist/about/index.html` exists (About page built; the actual path depends on Astro's `trailingSlash` setting — either is acceptable)
  - `dist/404.html` exists (the 404 page is built; CF Pages will auto-serve it for unmatched URLs)
  - `dist/sitemap-index.xml` and `dist/sitemap-0.xml` exist
  - `dist/robots.txt` exists and contains `Sitemap:`
  - `public/_redirects` exists and contains at least one line with `301` (the www → apex fallback)
  - The pre-paint script appears in `dist/index.html` BEFORE any `<link>` or `<style>` element (line number comparison — verified by `grep -n`)
  - The dev server returns `HTTP 404` for `/nonexistent-path` and the body contains the `咦？这里什么都没有…` text
  - The dev server returns Home with `进入次元` in the hero
  - The dev server returns About with at least one of `属性` / `技能` / `最喜欢的动画`
  - The commit message is `feat(phase-01): Home + About + 404 + Cloudflare Pages deploy`
  - The commit body lists the Cloudflare Pages deploy steps (8 numbered items from the action above)
  - The commit body references all 4 requirement IDs: PAGE-01, PAGE-02, INFRA-03, INFRA-06
  - `.planning/STATE.md` is updated: `completed_plans: 3`, the `By Phase` table shows `1. Foundation | 3/3`, and a new `Phase 1 Closeout` section is present
  - The Walking Skeleton invariant from `SKELETON.md` (pre-paint before stylesheet) is still verified after all 12 components + 2 pages are added
  </acceptance_criteria>
  <done>All 3 plans of Phase 1 are complete: scaffold + theme system + a11y + Home + About + 404 + Cloudflare Pages deploy config. The site is production-ready: `pnpm build` exits 0, the dist/ output is correct (Home, About, 404, sitemap, robots), the 404 returns HTTP 404 with the kawaii UI, the pre-paint FOUC-safe invariant holds in production, and the owner-side deploy steps are documented in the commit body. `.planning/STATE.md` is updated to mark Phase 1 complete.</done>
</task>

</tasks>

<security>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time YAML → persona card | `import persona from '../data/persona.yaml'` is read at build time only; no runtime user input renders into the persona card; safe by default (no XSS surface) |
| Build-time collection data → Home/About | `getCollection('articles')` and `getCollection('microblog')` return author-controlled content; the rendered HTML is server-side rendered at build time; no client-side mutation |
| 404 status code → search engine crawlers | `Astro.response.status = 404` is the canonical pattern; prevents soft-404s (PITFALLS P-21) that would otherwise be indexed as duplicates of the homepage |
| `public/_redirects` → www → apex redirect | Static redirect file; CF Pages serves it for matching paths; status `301` is a permanent redirect (cacheable) |
| Cloudflare Pages env vars → build runtime | `NODE_VERSION=22` and `NODE_OPTIONS=--max-old-space-size=4096` are build-only env vars; no secrets deployed in Phase 1 (Twikoo envId is empty placeholder only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed in plan 01-03; if YAML import fails, install `@rollup/plugin-yaml` (well-known Rollup plugin) — flagged as a [ASSUMED] install and the executor must verify against `npmjs.com/package/@rollup/plugin-yaml` before install |
| T-03-01 | Information Disclosure | persona.yaml avatar path | accept | The avatar `src` points to `/avatars/persona.png`; the file does not exist in Phase 1, so the `<img>` will 404 with a broken-image icon; no PII is exposed (the path is a placeholder); Phase 6 polish adds the real avatar file |
| T-03-02 | XSS | persona card content | mitigate | Persona data is YAML loaded at build; the only HTML rendering is text content via Astro's default escaping (`{persona.name.zh}` is escaped); no `{@html persona.bio}` usage; safe by default |
| T-03-03 | Repudiation | 404 status | mitigate | `Astro.response.status = 404` in `src/pages/404.astro` frontmatter (PITFALLS P-21); verified post-build via `curl -I` returning `HTTP/1.1 404 Not Found` |
| T-03-04 | Tampering | 404 page | accept | Astro 6 auto-generates `dist/404.html` from `src/pages/404.astro`; if the build emits it correctly, CF Pages serves it for unmatched paths; the file's existence is verified by `test -f dist/404.html` |
| T-03-05 | Spoofing | _redirects rule | mitigate | The `public/_redirects` rule forwards `www` to apex with `301` (permanent); browsers and crawlers will cache and not re-validate; the rule is intentionally narrow (only `https://www.merlinalex.me/*`) |
| T-03-06 | Denial of Service | build OOM | mitigate | `NODE_OPTIONS=--max-old-space-size=4096` is set in CF Pages env (CONTEXT D-12); the home page in Phase 1 has minimal images so OOM is unlikely, but the env var is cheap insurance |
| T-03-07 | Information Disclosure | sitemap contents | accept | The sitemap lists every page route; no private routes exist in Phase 1; safe |
</security>

<verification>
[Overall Phase 1 plan 01-03 checks]
- `pnpm build` exits 0 with Home, About, 404, sitemap, robots, and the _redirects file all in `dist/`
- `dist/index.html` contains: `<html lang="zh-CN">`, the pre-paint `<script is:inline>` BEFORE any stylesheet, `进入次元` in the hero, the empty-state copy OR a real article list (depending on the seed in plan 01-01)
- `dist/about.html` (or `dist/about/index.html`) contains: `属性` OR `技能` OR `最喜欢的动画` (the persona card sections)
- `dist/404.html` exists and `curl -I /nonexistent` returns `HTTP/1.1 404 Not Found` in dev mode
- `dist/sitemap-index.xml` + `dist/sitemap-0.xml` exist; `dist/robots.txt` references the sitemap
- `public/_redirects` contains the www → apex 301 rule
- Hard refresh of Home in browser (after deploy) shows the correct theme on first paint; theme switcher works; intensity badge works; no FOUC
- The owner-side Cloudflare Pages deploy steps are documented in the commit body
- `.planning/STATE.md` is updated to mark Phase 1 complete

[Overall Phase 1 checks — all 3 plans]
- Walking Skeleton invariant holds: pre-paint script before any stylesheet in `dist/index.html`
- All 10 Phase 1 requirement IDs are covered by at least one plan: PAGE-01, PAGE-02, ATM-04, INFRA-01, INFRA-02, INFRA-03, INFRA-06, A11Y-01, A11Y-02, SEO-01
- All 16 CONTEXT decisions D-01..D-16 are referenced by at least one plan's `must_haves.truths`
- The 4 ROADMAP success criteria are met:
  1. Visitor can open `https://merlinalex.me/` and see a styled Home with hero, intro slot, and notice/announcement bar (loads <2s on broadband)
  2. Visitor can navigate to `/about` and see the persona card with avatar, MBTI, zodiac, blood type, skill bars, favorite anime/character list, Q&A
  3. Visitor can toggle light/dark theme from the header; choice persists across reload with zero FOUC
  4. Visitor with `prefers-reduced-motion: reduce` sees no motion on first load; atmosphere intensity toggle is reachable, persisted in `localStorage`, and read by the gate (Phase 5 will plug consumers in)
  5. `sitemap.xml`, `robots.txt`, OG/Twitter cards, and the site is reachable via custom domain with HTTPS on Cloudflare Pages (post-deploy)
</verification>

<success_criteria>
[Measurable completion of plan 01-03 — and the entire Phase 1]
- The site at `https://merlinalex.me/` renders a styled Home (PAGE-01) with all 6 section slots in the locked order
- The site at `https://merlinalex.me/about` renders the persona card (PAGE-02) with all D-14 fields from `persona.yaml`
- Any non-existent URL at `https://merlinalex.me/nonexistent` returns `HTTP/2 404` with the kawaii 404 UI
- The theme switcher works on every page with no FOUC
- `prefers-reduced-motion: reduce` forces `data-atmo="off"` display without mutating `localStorage["atmo:level"]`
- `sitemap-index.xml` and `robots.txt` are reachable at the production URL
- The Walking Skeleton baseline from `SKELETON.md` is preserved
- The site is ready for the owner to push to `main` and trigger the first Cloudflare Pages production build
- `.planning/STATE.md` reflects Phase 1 complete; the project is ready for `/gsd-plan-phase 2` to plan Phase 2: Core Content (articles index, RSS, Twikoo, sticker pack)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
