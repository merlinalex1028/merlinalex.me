---
phase: 02-core-content
plan: 01
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/utils/reading-time.ts
  - src/utils/tag-extraction.ts
  - src/utils/related-posts.ts
  - src/components/articles/TagChips.astro
  - src/components/articles/ArticleListItem.astro
  - src/components/articles/StickyBadge.astro
  - src/pages/articles/index.astro
  - src/components/core/Nav.astro
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "Reader can visit /articles, see a single-column list of articles with category pills, and filter by tag using horizontal chip bar"
    - "Sticky articles float to the top of the list, then newest first"
    - "Reader can click any article in the list to navigate to the detail page"
    - "Nav '文章' link is enabled and clickable"
  artifacts:
    - path: "src/pages/articles/index.astro"
      provides: "Articles index page with tag filtering"
      min_lines: 40
    - path: "src/utils/reading-time.ts"
      provides: "CJK-aware reading time calculator"
      exports: ["calculateReadingTime"]
    - path: "src/utils/tag-extraction.ts"
      provides: "Tag extraction from article collection"
      exports: ["extractTags"]
    - path: "src/utils/related-posts.ts"
      provides: "Tag-overlap related posts algorithm"
      exports: ["findRelated"]
  key_links:
    - from: "src/pages/articles/index.astro"
      to: "src/components/articles/TagChips.astro"
      via: "import and render"
      pattern: "import TagChips"
    - from: "src/pages/articles/index.astro"
      to: "src/components/articles/ArticleListItem.astro"
      via: "import and render for each article"
      pattern: "import ArticleListItem"
    - from: "src/pages/articles/index.astro"
      to: "src/utils/tag-extraction.ts"
      via: "import extractTags"
      pattern: "extractTags"
---

<objective>
Build the articles index page with tag-based filtering, plus the utility modules and nav enablement.

Purpose: Deliver the article browsing experience — readers can visit /articles, see a list of articles sorted sticky-first then newest, filter by tag chips, and click into any article. This plan also creates the shared utility modules (reading-time, tag-extraction, related-posts) that the detail page in Plan 02 will consume.

Output: Articles index page, 3 utility modules, TagChips + ArticleListItem + StickyBadge components, Nav articles link enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-content/02-CONTEXT.md
@.planning/phases/02-core-content/02-RESEARCH.md
@.planning/phases/02-core-content/02-PATTERNS.md
@.planning/phases/02-core-content/02-UI-SPEC.md
@src/content.config.ts
@src/layouts/BaseLayout.astro
@src/components/home/LatestArticles.astro
@src/components/core/Nav.astro
@src/components/core/Footer.astro
@src/components/seo/SEOMeta.astro
@src/styles/global.css
@src/pages/index.astro
@src/content/articles/welcome.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→content | Article markdown is author-controlled (trusted) |
| URL→page | Tag filter via URL path processed at build time (no server-side injection vector) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Tag filter URL path | mitigate | Build-time static generation via `getStaticPaths` — no runtime query param processing; tag values are validated against known article tags at build time |
| T-02-02 | Tampering | Article content (markdown) | accept | Author-controlled content, no user input; Shiki handles code highlighting safely at build time |
| T-02-SC | Tampering | npm installs | mitigate | `@astrojs/rss`, `markdown-it`, `sanitize-html` all verified in RESEARCH.md package legitimacy audit — all APPROVED |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utility Modules + Tests</name>
  <files>
    package.json,
    pnpm-lock.yaml,
    src/utils/reading-time.ts,
    src/utils/tag-extraction.ts,
    src/utils/related-posts.ts
  </files>
  <read_first>
    @src/content.config.ts
    @src/styles/global.css
    @.planning/phases/02-core-content/02-PATTERNS.md
  </read_first>
  <behavior>
    - extractTags([article with tags ["tech","life"], article with tags ["tech","notes"]]) returns ["life","notes","tech"]
    - extractTags([]) returns []
    - calculateReadingTime("Hello world") returns 1 (minimum)
    - calculateReadingTime(long Chinese text with ~400 chars) returns ~1
    - calculateReadingTime(mixed CJK + English text) returns correct combined time
    - findRelated(current with tags ["a","b"], articles with ["a","b","c"] and ["a","c"], 2) returns sorted by shared tag count
    - findRelated(current, empty array, 3) returns []
  </behavior>
  <action>
    Install new dependencies (per D-03 for RSS, per D-02 for lightbox — medium-zoom installed in Plan 02-02 for detail page use): `pnpm add @astrojs/rss markdown-it sanitize-html`

    Create three utility modules:

    1. `src/utils/reading-time.ts` — export `calculateReadingTime(text: string): number`. Count CJK characters (regex `[一-鿿鿿぀-ゟ゠-ヿ]`) separately at ~400 chars/min, non-CJK words at ~200 words/min. Return `Math.max(1, Math.ceil(minutes))`.

    2. `src/utils/tag-extraction.ts` — export `extractTags(articles: CollectionEntry<'articles'>[]): string[]`. Collect unique tags from all articles into a Set, return sorted array.

    3. `src/utils/related-posts.ts` — export `findRelated(current: CollectionEntry<'articles'>, all: CollectionEntry<'articles'>[], limit?: number): CollectionEntry<'articles'>[]`. Score by shared tag count, take top N, fill remaining with most recent articles (exclude current by id).

    Write tests first (RED), then implement (GREEN):
    - Create `src/utils/__tests__/reading-time.test.ts` with test cases for pure English, pure CJK, mixed, and empty text
    - Create `src/utils/__tests__/tag-extraction.test.ts` with test cases for multiple articles, duplicates, and empty array
    - Create `src/utils/__tests__/related-posts.test.ts` with test cases for tag overlap scoring, limit, and fallback to recent
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/merlinalex.me && pnpm exec vitest run src/utils/__tests__/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/utils/reading-time.ts` exports `calculateReadingTime` function
    - `src/utils/tag-extraction.ts` exports `extractTags` function
    - `src/utils/related-posts.ts` exports `findRelated` function
    - All three test files pass: `pnpm exec vitest run src/utils/__tests__/` exits 0
  </acceptance_criteria>
  <done>
    - Three utility modules exist and are fully tested
    - `calculateReadingTime` handles CJK and English text correctly
    - `extractTags` deduplicates and sorts tags
    - `findRelated` scores by shared tag count with recent-article fallback
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Articles Index Page + List Components + Nav Enablement</name>
  <files>
    src/components/articles/TagChips.astro,
    src/components/articles/ArticleListItem.astro,
    src/components/articles/StickyBadge.astro,
    src/pages/articles/index.astro,
    src/components/core/Nav.astro
  </files>
  <read_first>
    @src/components/home/LatestArticles.astro
    @src/components/core/Nav.astro
    @src/styles/global.css
    @src/content.config.ts
    @src/pages/index.astro
    @src/utils/tag-extraction.ts
    @src/utils/reading-time.ts
    @.planning/phases/02-core-content/02-UI-SPEC.md
    @.planning/phases/02-core-content/02-PATTERNS.md
  </read_first>
  <behavior>
    - /articles page renders all non-draft articles in reverse chronological order
    - /articles/{tag} page renders only articles with that tag
    - Sticky articles appear before non-sticky articles regardless of date
    - Tag chips include "全部" plus all unique tags from articles
    - Selected tag chip has active styling (accent border + accent text)
    - Nav.astro "文章" link is enabled (not disabled)
  </behavior>
  <action>
    Create articles index page `src/pages/articles/index.astro`:
    - Uses `getStaticPaths` to pre-render one page per tag at build time (per D-01: no client-side JS for filtering)
    - Query: `getCollection('articles', ({ data }) => !data.draft)`
    - Extract tags via `extractTags`, create paths for each tag + an unfiltered path
    - Sort: sticky articles first (by `sticky` boolean descending), then by `publishedAt` descending
    - Render `TagChips` at top, then `ArticleListItem` for each article
    - Page shell: `BaseLayout title="文章" description="全部文章 — merlinalex.me" type="website"`

    Create `src/components/articles/TagChips.astro`:
    - Props: `tags: string[]`, `selected: string | null`
    - Horizontal scrollable bar: `overflow-x: auto`, `scrollbar-width: none`, `-webkit-overflow-scrolling: touch`
    - "全部" chip links to `/articles` (no path segment), active when `selected` is null
    - Each tag chip links to `/articles/{tag}`, active when `selected === tag`
    - Chip styling per UI-SPEC: pill shape (`border-radius: 9999px`), 14px, padding 6px 16px
    - Inactive: `background: var(--color-surface)`, `color: var(--color-fg-muted)`, `border: 1px solid var(--color-border)`
    - Active: `background: var(--color-accent-subtle)`, `color: var(--color-accent)`, `border: 1px solid var(--color-accent)`
    - Sticky below header: `position: sticky`, `top: 56px`, `z-index: 40`, `background: var(--color-bg)`

    Create `src/components/articles/ArticleListItem.astro`:
    - Props: `article: CollectionEntry<'articles'>`
    - Single row layout: `display: flex`, `gap: 1rem`, `padding: 1rem`, `border-radius: 20px`, `background: var(--color-surface)`, `border: 1px solid var(--color-border)`
    - Hover: `border-color: var(--color-accent)`, `box-shadow: 0 4px 16px rgba(255, 107, 157, 0.12)`
    - Content: title (16px, weight 600), excerpt (14px, 2-line clamp), date (14px, `zh-CN` locale), category pill
    - Category pill: `display: inline-block`, `padding: 2px 8px`, `border-radius: 9999px`, `background: var(--color-accent-subtle)`, `color: var(--color-accent)`
    - Link wraps entire card: `/articles/{article.id}`
    - If `article.data.sticky` is true, render `StickyBadge` before title

    Create `src/components/articles/StickyBadge.astro`:
    - No props (always renders "置顶" text)
    - Pill style: `display: inline-block`, `padding: 2px 8px`, `border-radius: 9999px`, `font-size: 12px`, `background: var(--color-accent)`, `color: white`

    Modify `src/components/core/Nav.astro`:
    - Change the articles nav item from `disabled: true` to remove the `disabled` property entirely (or set to `false`)
    - This enables the "文章" link in the header navigation
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/merlinalex.me && pnpm exec astro build 2>&1 | grep -E "(error|Error|articles)" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/articles/index.astro` exists and uses `getStaticPaths` for tag filtering
    - `src/components/articles/TagChips.astro` renders "全部" as first chip
    - `src/components/articles/ArticleListItem.astro` renders category pill with accent-subtle background
    - `src/components/articles/StickyBadge.astro` renders "置顶" text
    - `src/components/core/Nav.astro` articles item has no `disabled: true` (grep confirms `disabled` is absent or `false` for articles)
    - `dist/articles/index.html` exists after build
    - Build completes without errors
  </acceptance_criteria>
  <done>
    - Articles index page renders at /articles with tag chips, single-column list, sticky-first sort
    - Tag filtering works via /articles/{tag} URL paths (static generation)
    - Nav "文章" link is clickable and navigates to /articles
    - Build completes without errors
  </done>
</task>

</tasks>

<verification>
- `pnpm exec vitest run src/utils/__tests__/` — all utility tests pass
- `pnpm exec astro build` — build completes without errors
- `grep -c 'disabled.*true' src/components/core/Nav.astro` — articles link is enabled
- `ls dist/articles/` — contains `index.html` and at least one tag subdirectory
</verification>

<success_criteria>
1. Reader can visit /articles, see articles in single-column list with category pills, filter by tag chips
2. All utility tests pass
3. Nav "文章" link is enabled
4. Astro build completes without errors
</success_criteria>

<output>
Create `.planning/phases/02-core-content/02-01-SUMMARY.md` when done
</output>
