---
phase: 02-core-content
plan: 02
type: execute
wave: 1
depends_on:
  - 01-03
files_modified:
  - src/pages/articles/[id].astro
  - src/components/articles/ArticleTOC.astro
  - src/components/articles/ArticleNav.astro
  - src/components/articles/RelatedPosts.astro
  - src/components/articles/CopyCodeButton.astro
  - src/components/articles/ImageLightbox.astro
  - src/components/articles/ShareButtons.astro
  - src/components/articles/CopyrightFooter.astro
  - src/components/articles/TopButton.astro
  - src/components/articles/ReadingMeta.astro
  - src/content/articles/welcome.md
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements:
  - PAGE-03
  - PAGE-04

must_haves:
  truths:
    - "Reader can click any article and see the full detail page with TOC sidebar, syntax-highlighted code with copy button, reading time, and navigation"
    - "Reader can click images in articles to see them in a lightbox (medium-zoom) and dismiss with Escape"
    - "Reader can use prev/next cards at the bottom of an article to navigate between articles"
    - "Reader can click TOC headings on desktop to scroll to that section; TOC highlights current heading on scroll"
    - "Reader can share an article via copy-link, Twitter/X, or Weibo buttons"
    - "Reader can scroll to top via floating button after scrolling past viewport"
  artifacts:
    - path: "src/pages/articles/[id].astro"
      provides: "Article detail page with full UX"
      min_lines: 80
    - path: "src/components/articles/ArticleTOC.astro"
      provides: "Sticky sidebar TOC with scroll highlight"
    - path: "src/components/articles/CopyCodeButton.astro"
      provides: "Clipboard copy on code blocks"
    - path: "src/components/articles/ImageLightbox.astro"
      provides: "medium-zoom wrapper"
    - path: "src/components/articles/ReadingMeta.astro"
      provides: "Reading time + last-updated display"
  key_links:
    - from: "src/pages/articles/[id].astro"
      to: "src/components/articles/ArticleTOC.astro"
      via: "import and render when toc=true"
      pattern: "import ArticleTOC"
    - from: "src/pages/articles/[id].astro"
      to: "src/components/articles/CopyCodeButton.astro"
      via: "import with client:load"
      pattern: "client:load"
    - from: "src/pages/articles/[id].astro"
      to: "src/utils/reading-time.ts"
      via: "import calculateReadingTime"
      pattern: "calculateReadingTime"
    - from: "src/pages/articles/[id].astro"
      to: "src/utils/related-posts.ts"
      via: "import findRelated"
      pattern: "findRelated"
---

<objective>
Build the article detail page with all UX components: TOC sidebar, code copy button, image lightbox, reading time, prev/next navigation, related posts, share buttons, copyright footer, scroll-to-top button.

Purpose: Deliver the full article reading experience — the primary content consumption capability of the site. After this plan, a reader can click into any article and get a complete, polished reading experience.

Output: Article detail page, 9 new components, enriched sample article.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-content/02-CONTEXT.md
@.planning/phases/02-core-content/02-RESEARCH.md
@.planning/phases/02-core-content/02-PATTERNS.md
@.planning/phases/02-core-content/02-UI-SPEC.md
@src/content.config.ts
@src/layouts/BaseLayout.astro
@src/components/home/LatestArticles.astro
@src/components/core/ThemeSwitcher.astro
@src/styles/global.css
@src/content/articles/welcome.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author→content | Article markdown is author-controlled (trusted) |
| URL→page | Article ID resolved via `getStaticPaths` at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Article content (markdown) | accept | Author-controlled content, no user input; Shiki handles code highlighting safely at build time |
| T-02-03 | Tampering | medium-zoom library | accept | Mature library (8+ years), loaded from npm, no user input to zoom API |
| T-02-SC | Tampering | npm installs | mitigate | `medium-zoom` verified in RESEARCH.md package legitimacy audit — APPROVED |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Article Detail Page + All Components</name>
  <files>
    package.json,
    pnpm-lock.yaml,
    src/pages/articles/[id].astro,
    src/components/articles/ArticleTOC.astro,
    src/components/articles/ArticleNav.astro,
    src/components/articles/RelatedPosts.astro,
    src/components/articles/CopyCodeButton.astro,
    src/components/articles/ImageLightbox.astro,
    src/components/articles/ShareButtons.astro,
    src/components/articles/CopyrightFooter.astro,
    src/components/articles/TopButton.astro,
    src/components/articles/ReadingMeta.astro
  </files>
  <read_first>
    @src/layouts/BaseLayout.astro
    @src/components/home/LatestArticles.astro
    @src/components/core/ThemeSwitcher.astro
    @src/styles/global.css
    @src/content.config.ts
    @.planning/phases/02-core-content/02-UI-SPEC.md
    @.planning/phases/02-core-content/02-PATTERNS.md
  </read_first>
  <behavior>
    - /articles/welcome renders the article with title, reading time, TOC, and content
    - Article detail page shows "X 分钟阅读" reading time indicator
    - TOC sidebar visible on desktop (>1024px), collapsible accordion on mobile
    - TOC highlights current heading on scroll via IntersectionObserver
    - Code blocks have copy button that shows "已复制!" feedback for 2 seconds
    - Images in article content are zoomable via medium-zoom (click to expand, Escape to dismiss)
    - Prev/next navigation cards appear at bottom of article
    - Related posts grid shows up to 3 articles matched by shared tags
    - Share buttons (copy link, Twitter/X, Weibo) render as icon circles
    - Copyright footer shows "本文作者: merlinalex" and permalink
    - Top button appears after scrolling past viewport height
  </behavior>
  <action>
    Install medium-zoom dependency: `pnpm add medium-zoom` (other deps already installed in Plan 02-01).

    Create article detail page `src/pages/articles/[id].astro`:
    - `getStaticPaths`: fetch all non-draft articles from `getCollection('articles', ({ data }) => !data.draft)`, return `params: { id: article.id }, props: { article }`
    - `render(article)` to get `Content` and `headings`
    - Calculate reading time via `calculateReadingTime(article.body)` from `src/utils/reading-time.ts`
    - Find related articles via `findRelated(article, allArticles, 3)` from `src/utils/related-posts.ts`
    - Find prev/next by filtering all articles sorted by publishedAt, locating current index
    - Page shell: `BaseLayout title={article.data.title} description={article.data.description} type="article"`
    - Layout: two-column on desktop (>1024px) with TOC sidebar (220px) + article body (flex-1); single column on mobile
    - Article header: title (28px, weight 600), ReadingMeta, cover image (if set) with `border-radius: 12px`
    - Article body: `<article class="prose">` wrapping `<Content />`
    - Components in order after content: ShareButtons, CopyrightFooter, ArticleNav, RelatedPosts
    - TopButton rendered at page level (fixed position)

    Create `src/components/articles/ArticleTOC.astro`:
    - Props: `headings: { depth: number, slug: string, text: string }[]`
    - Filter to h2 and h3 only
    - Desktop (>1024px): `position: sticky`, `top: 72px`, `width: 220px`, `max-height: calc(100vh - 72px)`, `overflow-y: auto`
    - Mobile (<=1024px): collapsible accordion with "目录" toggle button, `border-radius: 12px`, `background: var(--color-surface)`
    - Item styling: 14px, `color: var(--color-fg-muted)`, `padding: 4px 0 4px 12px`, `border-left: 2px solid transparent`
    - Active item: `color: var(--color-accent)`, `border-left-color: var(--color-accent)`, `font-weight: 500`
    - h3 items indented 24px from h2
    - Client-side IntersectionObserver via inline `<script>`: `rootMargin: '-80px 0px -80px 0px'`, `threshold: 0`; observe all `article h2, article h3`; on intersect, update active class on TOC link
    - TOC link click: `element.scrollIntoView({ behavior: 'smooth', block: 'start' })`

    Create `src/components/articles/CopyCodeButton.astro`:
    - `client:load` directive (must be interactive immediately)
    - Inline script: query all `pre` elements within article, inject a copy button in top-right corner of each
    - Button: absolute positioned, `padding: 4px 8px`, `border-radius: 6px`, `font-size: 14px`, `background: var(--color-surface)`, `border: 1px solid var(--color-border)`
    - Default: clipboard icon + "复制" text
    - On click: `navigator.clipboard.writeText(pre.textContent)`, show "已复制!" with `color: var(--color-accent)`, revert after 2000ms
    - Fallback: `document.execCommand('copy')` with temporary textarea for older browsers
    - Listen for `astro:after-swap` to re-initialize after View Transitions
    - Code block `<pre>` container: `border-radius: 12px`, `overflow: hidden`, `margin: 1.5rem 0`, `position: relative`; copy button `opacity: 0` by default, `opacity: 1` on `pre:hover`

    Create `src/components/articles/ImageLightbox.astro`:
    - `client:idle` directive
    - Import `medium-zoom` from `'medium-zoom'`
    - Initialize on `article img` selector with `margin: 24`, `background: 'rgba(0, 0, 0, 0.8)'`
    - Listen for `astro:after-swap` to re-initialize
    - Article images styled: `border-radius: 8px`, `cursor: zoom-in`

    Create `src/components/articles/ArticleNav.astro`:
    - Props: `prev: CollectionEntry<'articles'> | undefined`, `next: CollectionEntry<'articles'> | undefined`
    - Two-card grid: `display: grid`, `grid-template-columns: 1fr 1fr`, `gap: 1rem`
    - Each card: `padding: 1rem`, `border-radius: 16px`, `background: var(--color-surface)`, `border: 1px solid var(--color-border)`
    - Label: 14px, `color: var(--color-fg-muted)`, "上一篇" / "下一篇"
    - Title: 14px, weight 500, `color: var(--color-fg)`, max 2 lines
    - Chevron SVG inline for direction indicator
    - If no prev or next, hide that card; remaining card takes full width

    Create `src/components/articles/RelatedPosts.astro`:
    - Props: `articles: CollectionEntry<'articles'>[]`
    - Section heading: "相关文章", 20px, weight 600
    - 3-card grid: `grid-template-columns: repeat(3, 1fr)`, `gap: 1rem`
    - Reuse card styling from `LatestArticles.astro` (same border-radius, surface bg, hover shadow effect)
    - Card content: title + date (no excerpt for compactness)
    - Mobile (<640px): single column stack

    Create `src/components/articles/ShareButtons.astro`:
    - Props: `title: string`, `url: string`
    - Horizontal row: `gap: 12px`
    - Three circular buttons (36px, `border-radius: 50%`): copy link (clipboard icon), Twitter/X (X icon), Weibo (Weibo icon)
    - Inline SVG icons (no icon library dependency, per UI-SPEC)
    - Copy link: `navigator.clipboard.writeText(url)`, show "已复制链接!" tooltip for 2s
    - Hover: `background: var(--color-accent-subtle)`, `border-color: var(--color-accent)`

    Create `src/components/articles/CopyrightFooter.astro`:
    - Props: `title: string`, `url: string`
    - Text: "本文作者: merlinalex · 本文链接: {permalink} · 转载请注明出处"
    - Permalink is an inline link with `color: var(--color-accent)`
    - Style: `padding: 1.5rem 0`, `border-top: 1px solid var(--color-border)`, `margin-top: 2rem`, 14px, `color: var(--color-fg-muted)`

    Create `src/components/articles/TopButton.astro`:
    - `client:idle` directive (needs to listen for scroll even when not visible)
    - Fixed position: `bottom: 2rem`, `right: 2rem`, `z-index: 40`
    - 44px circle: `background: var(--color-accent)`, `color: white`, `border-radius: 50%`
    - Chevron-up SVG icon (20px)
    - Visibility: hidden (`opacity: 0`, `pointer-events: none`) by default; show (`opacity: 1`, `pointer-events: auto`) when `window.scrollY > window.innerHeight`
    - Scroll listener throttled via `requestAnimationFrame`
    - Click: `window.scrollTo({ top: 0, behavior: 'smooth' })`
    - `aria-label="回到顶部"`
    - Hover: `background: var(--color-accent-hover)`, `transform: translateY(-2px)`

    Create `src/components/articles/ReadingMeta.astro`:
    - Props: `minutes: number`, `updatedAt: Date | undefined`
    - Horizontal layout: `gap: 1rem`, `margin: 0.75rem 0`
    - Show reading time: "{minutes} 分钟阅读"
    - Show last-updated only if `updatedAt` exists: "最后更新于 {date}" (zh-CN locale)
    - Separator: `·` (middle dot)
    - Style: 14px, `color: var(--color-fg-muted)`

    Prose styling for article body (scoped in `[id].astro` or a shared style):
    - Apply typography styles to `.prose` class: headings, paragraphs, code blocks, images, lists, blockquotes, tables, links
    - Max-width: 768px for article container
    - Body: 16px, line-height 1.5
    - h2: 24px, weight 600, margin-top 2rem
    - h3: 16px, weight 600, margin-top 1.5rem
    - Inline code: `background: var(--color-surface)`, `padding: 2px 6px`, `border-radius: 4px`, 14px
    - Images: `border-radius: 8px`, `max-width: 100%`
    - Links: `color: var(--color-accent)`, underline on hover
    - Blockquote: `border-left: 3px solid var(--color-accent-subtle)`, `padding-left: 1rem`, `color: var(--color-fg-muted)`
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/merlinalex.me && pnpm exec astro build 2>&1 | grep -E "(error|Error|warn|articles)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/pages/articles/[id].astro` exists with `getStaticPaths` and `render(article)`
    - `dist/articles/welcome/index.html` contains `class="toc"` or similar TOC container
    - `dist/articles/welcome/index.html` contains `copy` button or `clipboard` reference
    - `dist/articles/welcome/index.html` contains `medium-zoom` or `data-zoomable`
    - `dist/articles/welcome/index.html` contains "分钟阅读" text
    - `dist/articles/welcome/index.html` contains "相关文章" heading
    - `pnpm exec astro build` completes without errors
    - Article detail page has proper prose styling (headings, code blocks, images styled)
  </acceptance_criteria>
  <done>
    - Article detail page renders with TOC sidebar (desktop) or accordion (mobile)
    - Code blocks have syntax highlighting (Shiki) and copy button with "已复制!" feedback
    - Images zoom on click via medium-zoom, dismiss with Escape
    - Reading time displays in article header
    - Prev/next navigation cards work
    - Related posts grid shows articles matched by shared tags
    - Share buttons (copy link, Twitter/X, Weibo) render as circular icons
    - Copyright footer shows author + permalink
    - Scroll-to-top button appears after scrolling past viewport
  </done>
</task>

<task type="auto">
  <name>Task 2: Enrich Sample Article for Testing</name>
  <files>
    src/content/articles/welcome.md
  </files>
  <read_first>
    @src/content/articles/welcome.md
    @src/content.config.ts
    @.planning/phases/02-core-content/02-UI-SPEC.md
  </read_first>
  <action>
    Enrich `src/content/articles/welcome.md`:
    - Add multiple h2 and h3 headings (at least 3 h2, 2 h3) to test TOC
    - Add a fenced code block (```javascript or ```typescript) to test Shiki syntax highlighting + copy button
    - Add an image reference (can be a placeholder path like `/images/welcome-hero.png` or a public domain URL) to test lightbox
    - Ensure image has meaningful alt text (per D-05: all images must have alt text)
    - Keep existing frontmatter (title, publishedAt, tags, etc.); add `category: "notes"` if not present
    - Ensure enough CJK + English mixed text for reading time calculation testing
    - Add `toc: true` to frontmatter if not already present
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/merlinalex.me && pnpm exec astro build 2>&1 | grep -E "(error|Error)" | head -5; grep -c '##' src/content/articles/welcome.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/articles/welcome.md` has at least 3 h2 headings and 1 code block
    - Article has `toc: true` in frontmatter
    - Any image references have descriptive alt text
    - Build completes without errors
  </acceptance_criteria>
  <done>
    - welcome.md has sufficient content to test all article detail page features
    - Headings, code blocks, and images are present for TOC, copy-code, and lightbox testing
  </done>
</task>

</tasks>

<verification>
- `pnpm exec astro build` — build completes without errors
- `ls dist/articles/` — contains `index.html` and `welcome/index.html`
- `grep '分钟阅读' dist/articles/welcome/index.html` — reading time renders
- `grep '相关文章' dist/articles/welcome/index.html` — related posts section exists
</verification>

<success_criteria>
1. Reader can click into any article and see full detail page with all UX features
2. Astro build completes without errors
</success_criteria>

<output>
Create `.planning/phases/02-core-content/02-02-SUMMARY.md` when done
</output>
