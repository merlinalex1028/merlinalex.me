---
phase: 03-works-friend-links
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/friends.astro
  - src/components/friends/FriendCard.astro
  - src/components/friends/FriendSection.astro
  - src/components/friends/HealthBadge.astro
  - src/content/friends/friends.json
  - src/data/friends-health.json
autonomous: true
requirements:
  - PAGE-08
  - INFRA-08

must_haves:
  truths:
    - "Visitor can browse /friends and see friend cards grouped by category"
    - "Category order is: 技术 → 动漫 → 生活 → 其他"
    - "Within each category, featured friends sort to top"
    - "Each friend card shows avatar, site name, and one-line description"
    - "Offline friends show a red/gray '已离线' badge in top-right corner"
    - "Prominent '申请友链' button at bottom links to GitHub Issue template"
    - "Cards remain visible and clickable even when friend is offline"
  artifacts:
    - path: "src/pages/friends.astro"
      provides: "Friend links page with category sections"
      contains: "getCollection('friends')"
    - path: "src/components/friends/FriendCard.astro"
      provides: "Horizontal friend card with avatar, name, description"
      contains: "friend"
    - path: "src/components/friends/FriendSection.astro"
      provides: "Category section wrapper with heading"
      contains: "category"
    - path: "src/components/friends/HealthBadge.astro"
      provides: "Offline badge overlay component"
      contains: "已离线"
    - path: "src/content/friends/friends.json"
      provides: "Friend entries data"
      contains: "friends"
    - path: "src/data/friends-health.json"
      provides: "Health check status data (UI consumer)"
      contains: "status"
  key_links:
    - from: "src/pages/friends.astro"
      to: "src/data/friends-health.json"
      via: "import at build time for badge status"
      pattern: "import.*friends-health"
    - from: "src/components/friends/FriendCard.astro"
      to: "src/components/friends/HealthBadge.astro"
      via: "conditional render when offline"
      pattern: "HealthBadge.*isOffline"
    - from: "src/pages/friends.astro"
      to: "GitHub Issue template"
      via: "submission button href"
      pattern: "github.com.*issues.*new"
---

<objective>
Build the Friend Links page with category grouping, health-check badges, and submission flow.

Purpose: Visitors can browse the owner's friend circle, discover sites by category, and apply to become a friend link via GitHub Issue. Offline friends are visually marked but remain accessible.

Output: Working /friends page with category-sorted friend cards, health badge UI, submission CTA, and sample friend data.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-works-friend-links/03-CONTEXT.md
@.planning/phases/03-works-friend-links/03-RESEARCH.md
@src/content.config.ts
@src/components/articles/ArticleListItem.astro
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Friends page + FriendCard + FriendSection + sample data + submission CTA</name>
  <files>
    src/pages/friends.astro,
    src/components/friends/FriendCard.astro,
    src/components/friends/FriendSection.astro,
    src/content/friends/friends.json
  </files>
  <action>
Create the Friend Links page with category-grouped friend cards. Per D-04 (locked decision):

1. **src/content/friends/friends.json** — Sample friend data as JSON array. Include 6-8 friends across all 4 categories:
   - Category `tech` (2-3): e.g., `{ "name": "张三的博客", "url": "https://example.com", "avatar": "https://avatars.githubusercontent.com/u/1?v=4", "description": "全栈开发 · 技术分享", "category": "tech", "featured": true }`
   - Category `anime` (2): e.g., `{ "name": "二次元研究所", "url": "https://anime.example.com", "avatar": "https://avatars.githubusercontent.com/u/2?v=4", "description": "动漫资讯与评测", "category": "anime" }`
   - Category `life` (1-2): e.g., `{ "name": "小明的生活日记", "url": "https://life.example.com", "avatar": "https://avatars.githubusercontent.com/u/3?v=4", "description": "日常记录与摄影", "category": "life" }`
   - Category `other` (1): e.g., `{ "name": "创意工坊", "url": "https://craft.example.com", "avatar": "https://avatars.githubusercontent.com/u/4?v=4", "description": "手工与设计", "category": "other" }`
   Mark 1-2 friends as `featured: true`. Give each a unique `id` field (e.g., `"zhangsan-blog"`).

2. **src/components/friends/FriendCard.astro** — Horizontal card component. Props: `name: string`, `url: string`, `avatar?: string`, `description?: string`, `isOffline: boolean`. Renders:
   - `<a href={url} target="_blank" rel="noopener noreferrer" class="friend-card">` wrapping the entire card
   - Avatar: `<img>` with `width="48" height="48"`, `border-radius: 50%`, `loading="lazy"`, `alt={name}`. Fallback: colored circle with first character of name if no avatar URL.
   - Name: `<h3>` with `font-size: 16px; font-weight: 600; color: var(--color-fg); margin: 0;`
   - Description: `<p>` with `font-size: 14px; color: var(--color-fg-muted); margin: 0;` truncated to 1 line
   - Layout: `display: flex; align-items: center; gap: 12px; padding: 12px 16px; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 16px; text-decoration: none; transition: border-color 150ms, box-shadow 250ms;`
   - Hover: `border-color: var(--color-accent); box-shadow: 0 4px 16px rgba(255, 107, 157, 0.12);`
   - `position: relative;` on container for HealthBadge positioning

3. **src/components/friends/FriendSection.astro** — Category section wrapper. Props: `title: string`, `friends: array`. Renders:
   - `<section class="friend-section">` with `<h2>` category heading
   - Grid of FriendCards: `display: grid; grid-template-columns: 1fr; gap: 0.75rem;` with `@media (min-width: 640px) { grid-template-columns: repeat(2, 1fr); }`
   - Section spacing: `margin-bottom: 2rem;`

4. **src/pages/friends.astro** — Page using BaseLayout:
   - Import `getCollection('friends')` — note: the friends collection uses `file()` loader so `getCollection` returns one entry whose `data` is the full array. Access friends via `friends[0].data` (the array).
   - Category order: `['tech', 'anime', 'life', 'other']` with display names: `技术`, `动漫`, `生活`, `其他`
   - Group friends by category, sort within each category: `featured: true` first, then alphabetical by name
   - Render each category as a FriendSection (skip empty categories)
   - Page title: `<h1>友人帐</h1>` centered
   - Submission CTA at bottom: `<div class="submission-cta">` with a prominent button/link:
     ```html
     <a href="https://github.com/merlinalex/merlinalex.me/issues/new?template=friend-link.yml"
        target="_blank" rel="noopener noreferrer"
        class="submit-btn">
       申请友链
     </a>
     ```
     Style: `display: inline-block; padding: 12px 32px; background: var(--color-accent); color: white; border-radius: 9999px; font-weight: 600; text-decoration: none; transition: background 150ms;` Hover: `background: var(--color-accent-hover);`
     Wrapper: `text-align: center; margin-top: 2rem; padding: 2rem 0; border-top: 1px solid var(--color-border);`
  </action>
  <verify>
    <automated>pnpm exec astro check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - /friends shows friend cards grouped by category in order: 技术 → 动漫 → 生活 → 其他
    - Featured friends sort to top within each category
    - Each card shows avatar (or fallback), name, and description
    - Cards are clickable and open friend site in new tab
    - "申请友链" button at bottom links to GitHub Issue template
    - Grid is responsive: 1 column mobile, 2 columns desktop
  </done>
</task>

<task type="auto">
  <name>Task 2: HealthBadge + friends-health.json + badge integration</name>
  <files>
    src/components/friends/HealthBadge.astro,
    src/data/friends-health.json,
    src/pages/friends.astro
  </files>
  <action>
Add the health-check badge system for offline friends. Per D-04 (locked decision) and INFRA-08 (UI badge stub — the GitHub Action producer ships in Phase 6):

1. **src/data/friends-health.json** — Health check data file. Initial content with schema that Phase 6's GitHub Action will populate:
   ```json
   {
     "https://offline-example.com": {
       "status": "offline",
       "lastCheck": "2026-06-03T00:00:00Z",
       "statusCode": 0,
       "responseTime": 0
     }
   }
   ```
   Add one sample offline entry pointing to a dummy URL. This demonstrates the badge UI. The GitHub Action in Phase 6 will overwrite this file with real data.

   TypeScript interface for the JSON (export from a types file or inline in the page):
   ```typescript
   interface FriendHealth {
     status: 'online' | 'offline';
     lastCheck: string;
     statusCode: number;
     responseTime: number;
   }
   type FriendsHealthData = Record<string, FriendHealth>;
   ```

2. **src/components/friends/HealthBadge.astro** — Offline badge overlay component. Props: `isOffline: boolean`. When `isOffline` is true, renders:
   - `<span class="health-badge" aria-label="已离线">已离线</span>`
   - CSS: `position: absolute; top: 8px; right: 8px; padding: 2px 8px; font-size: 12px; font-weight: 500; background: var(--color-fg-muted); color: var(--color-bg); border-radius: 9999px; opacity: 0.8; z-index: 10;`
   - When `isOffline` is false, renders nothing

3. **src/pages/friends.astro** — Update the friends page to integrate HealthBadge:
   - Import `friends-health.json` at build time: `import healthData from '../../data/friends-health.json';`
   - For each friend, determine `isOffline`: check if `healthData[friend.url]?.status === 'offline'`
   - Pass `isOffline` prop to FriendCard
   - Update FriendCard to accept `isOffline` prop and conditionally render `<HealthBadge isOffline={isOffline} />` inside the card container
   - Offline cards remain fully visible and clickable — the badge is informational, not blocking
   - Add one sample friend entry in `friends.json` with a URL matching the offline entry in `friends-health.json` to demonstrate the badge
  </action>
  <verify>
    <automated>pnpm exec astro check 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Offline friends show a "已离线" badge in top-right corner of their card
    - Online friends show no badge
    - Offline cards remain visible and clickable
    - friends-health.json has correct schema with status, lastCheck, statusCode, responseTime
    - Badge uses absolute positioning relative to card container
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| friends.json -> page | Content author controls friend URLs and avatars |
| friends-health.json -> badge | Phase 6 Action controls health status data |
| Friend URL -> external site | Clicking a friend card navigates to external site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-03-05 | Tampering | Friend link URLs | mitigate | Zod `z.string().url()` validates all friend URLs in content schema; `rel="noopener noreferrer"` on all external links |
| T-03-06 | Evasion | Friend avatar URLs | accept | Avatars served from external CDN (GitHub avatars); content author trusted; images not user-uploaded |
| T-03-07 | Information Disclosure | friends-health.json | accept | Health status is public data (friend sites are public); no sensitive information exposed |
</threat_model>

<verification>
- `pnpm exec astro check` passes with no errors
- /friends renders friend cards grouped by category
- Category order is correct: 技术 → 动漫 → 生活 → 其他
- Featured friends sort to top within each category
- Offline friends show "已离线" badge
- "申请友链" button links to GitHub Issue template
- Cards are responsive: 1 column mobile, 2 columns desktop
</verification>

<success_criteria>
- Friend cards grouped by category with correct sort order
- Health badge shows on offline friends with correct positioning
- Submission CTA links to GitHub Issue template
- friends-health.json schema matches what Phase 6 Action will produce
- All friend links open in new tab with noopener noreferrer
</success_criteria>

<output>
Create `.planning/phases/03-works-friend-links/03-03-SUMMARY.md` when done
</output>
