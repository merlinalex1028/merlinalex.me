---
phase: 04-community-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/microblog/index.astro
  - src/pages/microblog/data.json.ts
  - src/components/microblog/MicroblogCard.astro
  - src/content/microblog/hello-world.md
  - src/components/core/Nav.astro
autonomous: true
requirements: [PAGE-09]

must_haves:
  truths:
    - "Visitor can view /microblog and see paginated 说说 cards with date, content, mood emoji, and tags (per D-01)"
    - "Visitor can click 加载更多 to load the next batch of 10 posts without page reload (per D-01)"
    - "Visitor can click a microblog image thumbnail to open a medium-zoom lightbox (per D-01)"
    - "Posts older than 180 days show with 归档 tag and reduced opacity (per D-01)"
    - "Home page shows only the 5 latest microblog entries (per D-01)"
    - "Nav links for 说说, 时间线, and 归档 are all enabled and navigate to their pages"
  artifacts:
    - path: "src/pages/microblog/index.astro"
      provides: "Microblog feed page with initial 10 posts and load-more button"
    - path: "src/pages/microblog/data.json.ts"
      provides: "Static JSON endpoint with all microblog posts for client-side pagination"
    - path: "src/components/microblog/MicroblogCard.astro"
      provides: "Individual microblog post card with mood, tags, images, archive indicator"
    - path: "src/content/microblog/hello-world.md"
      provides: "Sample microblog post for build verification"
  key_links:
    - from: "src/pages/microblog/index.astro"
      to: "src/pages/microblog/data.json.ts"
      via: "client-side fetch for pagination data"
      pattern: "fetch.*microblog/data"
    - from: "src/components/microblog/MicroblogCard.astro"
      to: "medium-zoom"
      via: "image click handler"
      pattern: "mediumZoom|medium-zoom"
    - from: "src/pages/microblog/index.astro"
      to: "src/components/microblog/MicroblogCard.astro"
      via: "component import and rendering"
      pattern: "import.*MicroblogCard"
---

<objective>
Microblog feed (PAGE-09) + Nav enablement for 说说/时间线/归档 — deliver a paginated 说说 stream with image lightbox, mood emoji, tags, 180-day auto-archive, home page 5-latest cap, and enable all Phase 4 nav links.

Purpose: The microblog is the "alive" heartbeat of the site — short-form posts with personality that make the site feel inhabited. Nav consolidation prevents file conflicts across Wave 1 plans.
Output: Working /microblog page with client-side pagination, image lightbox, archive indicators, home page integration, and all nav links enabled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-community-search/04-CONTEXT.md
@.planning/phases/04-community-search/04-RESEARCH.md
@src/content.config.ts
@src/components/articles/TagChips.astro
@src/components/articles/ImageLightbox.astro
@src/components/home/LatestMicroblog.astro
@src/pages/index.astro
@src/components/core/Nav.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: MicroblogCard + sample content + JSON endpoint + feed page (per D-01)</name>
  <files>
    src/content/microblog/hello-world.md,
    src/components/microblog/MicroblogCard.astro,
    src/pages/microblog/data.json.ts,
    src/pages/microblog/index.astro
  </files>
  <read_first>
    src/content.config.ts,
    src/components/articles/TagChips.astro,
    src/components/articles/ImageLightbox.astro,
    src/components/home/LatestMicroblog.astro,
    src/components/core/Nav.astro,
    src/layouts/BaseLayout.astro
  </read_first>
  <action>
    Per D-01 (card-based layout, page-based pagination with 加载更多, 180-day auto-archive, image lightbox):

    **1. Create sample microblog post** at `src/content/microblog/hello-world.md`:
    - Frontmatter: publishedAt: 2026-06-03, content: 这是我的第一条说说！今天天气真好，适合写代码 ☀️, mood: 😊, tags: [日常, 开心]
    - Schema matches microblog in content.config.ts (publishedAt, content, mood, tags, images)

    **2. Create `src/components/microblog/MicroblogCard.astro`:**
    - Props: `post` (CollectionEntry<'microblog'>), `isArchived` (boolean, default false)
    - Card layout per D-01: date at top (YYYY-MM-DD, zh-CN locale), content text, mood emoji badge (if present), tag chips row (if tags exist), image thumbnail grid (max 3 images per D-01, click opens medium-zoom)
    - Archive styling per D-01: when `isArchived` true, add "归档" badge (small chip, muted color) and `opacity: 0.7` on card
    - Image thumbnails: `<img>` with `loading="lazy"`, `decoding="async"`, max 3 shown with "+N" overflow indicator
    - Reuse medium-zoom pattern from `ImageLightbox.astro`: import `medium-zoom` in `<script>`, target `.microblog-card img`
    - CSS variables: `--color-surface`, `--color-border`, `--color-fg`, `--color-fg-muted`, `--color-accent`, `--radius-md`
    - Date threshold: parent computes `isArchived = (Date.now() - publishedAt) > 180 * 24 * 60 * 60 * 1000`

    **3. Create `src/pages/microblog/data.json.ts`:**
    - Astro API endpoint returning all microblog posts as JSON at build time
    - Import `getCollection('microblog')`, sort by publishedAt descending
    - Map each post to: `{ id, publishedAt (ISO string), content, mood, tags, images }`
    - Export `GET` returning `new Response(JSON.stringify(posts), { headers: { 'Content-Type': 'application/json' } })`
    - Builds to `dist/microblog/data.json` for client-side pagination

    **4. Create `src/pages/microblog/index.astro`:**
    - Import `getCollection('microblog')` for initial SSR of first 10 posts
    - Sort by publishedAt descending, slice first 10
    - Compute `isArchived` per D-01: `(Date.now() - post.data.publishedAt.getTime()) > 180 * 24 * 60 * 60 * 1000`
    - Render initial posts using MicroblogCard
    - "加载更多" button per D-01 (hidden if total <= 10)
    - Client-side `<script>`: on click fetch `/microblog/data.json`, slice next 10, create card HTML, append to feed, hide button when all loaded
    - Wrap content in `<div data-pagefind-filter="exclude">` to exclude from search
    - Use BaseLayout with title "说说 — merlinalex.me"
  </action>
  <verify>
    <automated>pnpm exec astro build 2>&1 | tail -5 && test -f dist/microblog/index.html && test -f dist/microblog/data.json && echo "BUILD OK"</automated>
  </verify>
  <done>
    - /microblog page renders with sample post card showing date, content, mood emoji, tags
    - /microblog/data.json returns valid JSON array of posts
    - 加载更多 button present when >10 posts exist
    - Images have loading="lazy" and data-zoom attributes
    - Microblog content wrapped in data-pagefind-filter="exclude"
    - Archive threshold: posts >180 days get 归档 badge + reduced opacity (per D-01)
  </done>
</task>

<task type="auto">
  <name>Task 2: LatestMicroblog home page integration + all Nav enablement (per D-01)</name>
  <files>
    src/components/home/LatestMicroblog.astro,
    src/pages/index.astro,
    src/components/core/Nav.astro
  </files>
  <read_first>
    src/components/home/LatestMicroblog.astro,
    src/pages/index.astro,
    src/components/microblog/MicroblogCard.astro,
    src/components/core/Nav.astro
  </read_first>
  <action>
    Per D-01 (home page cap: only 5 latest microblog posts, linking to full /microblog):

    **1. Update `src/components/home/LatestMicroblog.astro`:**
    - Keep existing props interface (`posts = []`)
    - Upgrade card styling to show mood emoji, tag chips, and 1 image thumbnail per card (compact home version)
    - Add "查看更多 →" link at bottom pointing to `/microblog`
    - Maintain existing empty state with mascot-peek styling
    - Keep self-contained — do NOT import MicroblogCard (home cards are simpler)

    **2. Verify `src/pages/index.astro`:**
    - Existing code already fetches latest 5 microblog posts and passes to LatestMicroblog — verify works with new sample content
    - No changes needed to data fetching logic (already slices to 5, sorts by publishedAt)

    **3. Enable ALL Phase 4 nav links** in `src/components/core/Nav.astro` (consolidated from Plans 04-01/04-03/04-04 to prevent file conflicts across Wave 1):
    - Add `{ label: '说说', href: '/microblog' }` before "作品" in items array
    - Find existing `{ label: '时间线', href: '/timeline', disabled: true }` entry — remove `disabled: true` property entirely (or set to false) to enable the timeline link
    - Add `{ label: '归档', href: '/archive' }` after "友人" in items array
    - This consolidates all nav changes from Plans 04-01, 04-03, and 04-04 into this single plan
  </action>
  <verify>
    <automated>pnpm exec astro build 2>&1 | tail -5 && grep -c "查看更多" dist/index.html && grep -c "/microblog" dist/index.html && grep -c "/timeline" dist/index.html && grep -c "/archive" dist/index.html && echo "NAV+HOME OK"</automated>
  </verify>
  <done>
    - Home page shows latest 5 microblog posts with mood emoji and truncated content
    - "查看更多 →" link navigates to /microblog
    - Empty state works when no microblog posts exist
    - Nav shows "说说" link pointing to /microblog
    - Nav shows "时间线" link enabled (not disabled) pointing to /timeline
    - Nav shows "归档" link pointing to /archive
    - Build completes without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → client | Microblog content from markdown files rendered to static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | microblog content | mitigate | Zod schema validates all frontmatter fields; content rendered as static HTML |
| T-04-01-02 | Information Disclosure | data.json endpoint | accept | Public blog content, no sensitive data in microblog posts |
</threat_model>

<verification>
1. `pnpm exec astro build` succeeds
2. `/microblog/index.html` exists in dist
3. `/microblog/data.json` returns valid JSON with post array
4. Home page (`dist/index.html`) contains latest microblog entries
5. Nav contains "说说", "时间线" (enabled), and "归档" links
6. Sample post renders with mood emoji, tags, and date
</verification>

<success_criteria>
- Visitor can browse /microblog with paginated 说说 stream (per D-01)
- Load more button fetches next batch from static JSON endpoint
- Posts >180 days show archive indicator
- Home page shows 5 latest microblog posts
- Image lightbox works on microblog images
- Nav "说说" link is enabled
- Nav "时间线" link is enabled (was disabled)
- Nav "归档" link is enabled (new)
</success_criteria>

<output>
Create `.planning/phases/04-community-search/04-01-SUMMARY.md` when done
</output>
