---
phase: 04-community-search
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/bangumi.ts
  - src/utils/__tests__/bangumi.test.ts
  - src/content.config.ts
  - src/components/bangumi/BangumiCard.astro
  - src/components/bangumi/BangumiStatusTabs.astro
  - src/pages/anime.astro
  - src/pages/books.astro
  - src/pages/music.astro
  - src/data/bangumi.json
  - src/data/bangumi-override.json
  - .env.example
autonomous: true
requirements: [PAGE-10, INFRA-05]

must_haves:
  truths:
    - "Visitor can visit /anime and see a card grid of anime with cover image, title, rating, and progress"
    - "Visitor can toggle between 全部/在看/看过/想看 status tabs to filter the list"
    - "Visitor can visit /books and /music with the same card grid and status tabs"
    - "Bangumi data is fetched at build time and cached to src/data/bangumi.json"
    - "Manual overrides from bangumi-override.json take priority over API data"
    - "Graceful fallback when Bangumi API is unavailable — show cached data or empty state"
  artifacts:
    - path: "src/utils/bangumi.ts"
      provides: "Bangumi API fetch, cache read/write, override merge logic"
      exports: ["fetchBangumiData", "readCache", "mergeOverrides", "BANGUMI_TYPES"]
    - path: "src/components/bangumi/BangumiCard.astro"
      provides: "Media card with cover, title, rating stars, progress bar, status tag"
    - path: "src/components/bangumi/BangumiStatusTabs.astro"
      provides: "Tab toggle for status filtering (全部/在看/看过/想看)"
    - path: "src/pages/anime.astro"
      provides: "Anime list page with BangumiCard grid and status tabs"
    - path: "src/pages/books.astro"
      provides: "Books list page"
    - path: "src/pages/music.astro"
      provides: "Music list page"
    - path: "src/data/bangumi.json"
      provides: "Cached Bangumi collection data"
    - path: "src/data/bangumi-override.json"
      provides: "Manual override entries merged at build time"
  key_links:
    - from: "src/pages/anime.astro"
      to: "src/data/bangumi.json"
      via: "file() loader in content collection"
      pattern: "getCollection.*anime"
    - from: "src/utils/bangumi.ts"
      to: "api.bgm.tv/v0/"
      via: "fetch at build time"
      pattern: "api\\.bgm\\.tv"
    - from: "src/content.config.ts"
      to: "src/data/bangumi.json"
      via: "file() loader"
      pattern: "file.*bangumi"
---

<objective>
Bangumi-driven anime/book/music lists (PAGE-10, INFRA-05) — build-time fetch from Bangumi API v0, 12h TTL cache, manual override support, card grid with status tabs.

Purpose: The anime/book/music lists are the media shelf of the site — showing what the owner is watching, has watched, and wants to watch, driven by real Bangumi data.
Output: Working /anime, /books, /music pages with cover art, ratings, progress, and status filtering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-community-search/04-CONTEXT.md
@.planning/phases/04-community-search/04-RESEARCH.md
@src/content.config.ts
@src/components/articles/TagChips.astro
@src/components/core/Nav.astro
@src/layouts/BaseLayout.astro
@.env.example
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Bangumi fetch utility with TDD + content schema + data files</name>
  <files>
    src/utils/__tests__/bangumi.test.ts,
    src/utils/bangumi.ts,
    src/content.config.ts,
    src/data/bangumi.json,
    src/data/bangumi-override.json,
    .env.example
  </files>
  <read_first>
    src/content.config.ts,
    src/utils/reading-time.ts,
    .env.example
  </read_first>
  <behavior>
    - fetchBangumiData(username, subjectType) fetches all pages from Bangumi API v0, returns flat array of BangumiItem
    - readCache(filePath) reads JSON file, returns parsed data or empty array if file missing/corrupt
    - mergeOverrides(items, overrides) merges override entries by subjectId, override wins
    - BANGUMI_TYPES maps: { anime: 2, book: 1, music: 3 }
    - Each BangumiItem has: subjectId, name, nameCn, cover, score, rate, type (1=wish/2=done/3=doing), epStatus, volStatus, eps, updatedAt
    - fetchBangomiData handles HTTP errors gracefully, returns empty array on failure
    - fetchBangumiData sets User-Agent header per bgm.tv policy
    - Cache staleness check: readCache returns { data, isStale } where isStale = true if file mtime > 12h
  </behavior>
  <action>
    **RED phase:**

    **1. Create test file** at `src/utils/__tests__/bangumi.test.ts`:
    - Test: `fetchBangumiData` returns empty array when API returns error (mock fetch to return 500)
    - Test: `fetchBangumiData` flattens paginated results (mock fetch to return 2 pages)
    - Test: `fetchBangumiData` sets User-Agent header
    - Test: `readCache` returns empty array for missing file
    - Test: `readCache` returns parsed data for valid file
    - Test: `readCache` marks data as stale when file is older than 12h
    - Test: `mergeOverrides` replaces matching items by subjectId
    - Test: `mergeOverrides` adds new override items not in API data
    - Test: `BANGUMI_TYPES` maps correctly: anime=2, book=1, music=3
    - Use `vi.mock('node:fs')` and `vi.stubGlobal('fetch', ...)` for mocking
    - Run tests — they MUST fail (RED)

    **GREEN phase:**

    **2. Create `src/utils/bangumi.ts`:**
    - Export interface `BangumiItem` with fields: subjectId (number), name (string), nameCn (string), cover (string URL), score (number), rate (number), type (number 1-5), epStatus (number), volStatus (number), eps (number), updatedAt (string ISO), comment (string optional), tags (string array)
    - Export `BANGUMI_TYPES` constant: `{ anime: 2, book: 1, music: 3 }` as const
    - Export `async function fetchBangumiData(username: string, subjectType: number): Promise<BangumiItem[]>`:
      - Paginate with limit=50, offset incrementing
      - Fetch `https://api.bgm.tv/v0/users/${username}/collections?subject_type=${subjectType}&limit=${limit}&offset=${offset}`
      - Headers: `{ 'User-Agent': 'merlinalex.me/1.0 (Astro build)' }`
      - Flatten `data.data` array, map each item to BangumiItem shape (extract from nested `subject` object)
      - Break when `allItems.length >= data.total` or response not ok
      - Wrap in try/catch, return empty array on error
    - Export `function readCache(filePath: string): { data: BangumiItem[]; isStale: boolean }`:
      - Read file with `fs.readFileSync`, parse JSON
      - Check `fs.statSync(filePath).mtimeMs` against `Date.now() - 12 * 60 * 60 * 1000`
      - Return `{ data: parsed, isStale }` or `{ data: [], isStale: true }` on error
    - Export `function mergeOverrides(items: BangumiItem[], overrides: BangumiItem[]): BangumiItem[]`:
      - Create Map from items by subjectId
      - For each override, set/replace in map
      - Return array from map values

    **3. Update `src/content.config.ts`:**
    - Replace the placeholder anime/books/music schemas with proper Zod validation:
    ```typescript
    const bangumiItemSchema = z.object({
      subjectId: z.number(),
      name: z.string(),
      nameCn: z.string().default(''),
      cover: z.string().url(),
      score: z.number().default(0),
      rate: z.number().default(0),
      type: z.number(), // 1=wish, 2=done, 3=doing, 4=on_hold, 5=dropped
      epStatus: z.number().default(0),
      volStatus: z.number().default(0),
      eps: z.number().default(0),
      updatedAt: z.string().optional(),
      comment: z.string().optional(),
      tags: z.array(z.string()).default([]),
    });
    ```
    - Update anime, books, music collections to use `z.array(bangumiItemSchema).default([])` — keep `file()` loader pointing to `./src/content/{type}/list.json`

    **4. Create data files:**
    - `src/data/bangumi.json`: empty object `{ "anime": [], "books": [], "music": [] }`
    - `src/data/bangumi-override.json`: empty object `{ "anime": [], "books": [], "music": [] }`
    - Update `src/content/anime/list.json`, `src/content/books/list.json`, `src/content/music/list.json`: keep as `[]` (will be populated by prebuild script)

    **5. Update `.env.example`:**
    - Add `BANGUMI_USERNAME=` with comment explaining it's the Bangumi user ID for collection fetch

    **6. Add scripts to `package.json`:**
    - Add `"bangumi:refresh": "node --import tsx src/scripts/fetch-bangumi.ts"` or equivalent
    - Add `"prebuild": "node --import tsx src/scripts/fetch-bangumi.ts"` (runs before every build)

    Run tests — they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/huangjingping/i/merlinalex.me && pnpm vitest run src/utils/__tests__/bangumi.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - All bangumi utility tests pass
    - BangumiItem interface exported with all required fields
    - fetchBangumiData handles pagination and errors gracefully
    - readCache returns stale flag based on 12h TTL
    - mergeOverrides correctly replaces/adds entries
    - Content schemas updated with proper Zod validation
    - .env.example includes BANGUMI_USERNAME
  </done>
</task>

<task type="auto">
  <name>Task 2: BangumiCard + StatusTabs + anime/books/music pages</name>
  <files>
    src/components/bangumi/BangumiCard.astro,
    src/components/bangumi/BangumiStatusTabs.astro,
    src/pages/anime.astro,
    src/pages/books.astro,
    src/pages/music.astro
  </files>
  <read_first>
    src/content.config.ts,
    src/components/articles/TagChips.astro,
    src/pages/works/projects/index.astro,
    src/layouts/BaseLayout.astro,
    src/components/core/Nav.astro
  </read_first>
  <action>
    **1. Create `src/components/bangumi/BangumiCard.astro`:**
    - Props: `item` (BangumiItem shape from content collection)
    - Card layout: cover image (left or top), title (nameCn if not empty, else name), score badge (e.g., "8.5"), progress text (e.g., "12/24 集"), status tag (在看/看过/想看 based on item.type: 3=在看, 2=看过, 1=想看)
    - Cover image: `loading="lazy"`, `decoding="async"`, rounded corners, aspect-ratio preserved
    - Score display: star icon or numeric badge with accent color
    - Progress: `epStatus/eps` format, only show if eps > 0
    - Use CSS variables: `--color-surface`, `--color-border`, `--color-fg`, `--color-accent`, `--radius-md`
    - Card hover: subtle shadow or scale effect (1.02x)
    - Responsive: 1 column mobile, 2 columns tablet, 3-4 columns desktop (match works/projects grid pattern)

    **2. Create `src/components/bangumi/BangumiStatusTabs.astro`:**
    - Props: `selected` (string: 'all' | 'doing' | 'done' | 'wish'), `counts` (object with all/doing/done/wish numbers)
    - Render 4 tabs: 全部, 在看, 看过, 想看
    - Each tab is a button with active state styling (matching TagChips.astro pattern)
    - Client-side: click handler sets `data-status` attribute on parent container, toggles visibility of cards by status
    - No page reload — pure client-side filtering using data attributes on cards
    - Use the same chip styling as TagChips.astro (rounded pills, accent color when active)

    **3. Create `src/pages/anime.astro`:**
    - Import `getCollection('anime')` to get all anime items
    - Import `BangumiCard` and `BangumiStatusTabs`
    - Sort items: 在看 first (type=3), then 想看 (type=1), then 看过 (type=2), then others
    - Compute counts for each status
    - Render `BangumiStatusTabs` with counts
    - Render grid of `BangumiCard` for each item, with `data-status` attribute on each card for client-side filtering
    - Client-side `<script>`: listen for tab clicks, filter cards by `data-status` attribute, show/hide with CSS
    - Use `BaseLayout` with title "追番 — merlinalex.me"
    - Add `data-pagefind-filter="exclude"` to exclude from search

    **4. Create `src/pages/books.astro`:**
    - Same pattern as anime.astro but with `getCollection('books')`
    - Title "阅读 — merlinalex.me"
    - Adapt labels: 在读/读过/想读 instead of 在看/看过/想看

    **5. Create `src/pages/music.astro`:**
    - Same pattern as anime.astro but with `getCollection('music')`
    - Title "音乐 — merlinalex.me"
    - Adapt labels: 在听/听过/想听

    **6. Update Nav** in `src/components/core/Nav.astro`:
    - Do NOT add nav links for anime/books/music (they are sub-pages, not primary nav)
    - These pages are accessible from the About page or via direct URL
  </action>
  <verify>
    <automated>pnpm exec astro build 2>&1 | tail -5 && test -f dist/anime/index.html && test -f dist/books/index.html && test -f dist/music/index.html && echo "BUILD OK"</automated>
  </verify>
  <done>
    - /anime, /books, /music pages all build successfully
    - BangumiCard shows cover, title, score, progress, status tag
    - Status tabs filter cards client-side without page reload
    - Empty state shown when no Bangumi data (API not configured)
    - Pages excluded from Pagefind search index
    - Cards are responsive (1-col mobile, 2-3 col desktop)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bangumi API → build cache | External API response written to local JSON file |
| Cache file → page render | Cached data rendered as static HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | Bangumi API response | mitigate | Zod schema validates all cached data before writing; invalid fields rejected |
| T-04-02-02 | Information Disclosure | BANGUMI_USERNAME env var | accept | Public Bangumi profile, no sensitive data |
| T-04-02-03 | Denial of Service | Bangumi API rate limit | mitigate | Cache to JSON with 12h TTL; only refresh when stale or manual trigger |
</threat_model>

<verification>
1. `pnpm vitest run src/utils/__tests__/bangumi.test.ts` passes all tests
2. `pnpm exec astro build` succeeds
3. `/anime/index.html`, `/books/index.html`, `/music/index.html` all exist in dist
4. Content schemas validate BangumiItem shape
5. .env.example includes BANGUMI_USERNAME
</verification>

<success_criteria>
- Bangumi fetch utility passes all unit tests
- /anime, /books, /music pages render with card grid
- Status tabs filter cards client-side
- Bangumi data cached to JSON with 12h TTL
- Manual overrides merge correctly
- Graceful fallback when API unavailable
</success_criteria>

<output>
Create `.planning/phases/04-community-search/04-02-SUMMARY.md` when done
</output>
