---
phase: 04-community-search
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/timeline/TimelineEntry.astro
  - src/components/timeline/TimelineYear.astro
  - src/pages/timeline.astro
  - src/content/timeline/2024-start.mdx
  - src/content/timeline/2024-blog.mdx
  - src/content/timeline/2026-site.mdx
autonomous: true
requirements: [PAGE-11]

must_haves:
  truths:
    - "Visitor can scroll /timeline and see year-by-year milestones in a vertical alternating-side layout"
    - "Timeline entries show date, title, description, optional image, and optional link"
    - "Entries are grouped by year with year headers (most recent year first)"
    - "Left and right sides alternate for each entry within a year"
  artifacts:
    - path: "src/pages/timeline.astro"
      provides: "Timeline page with year-grouped alternating entries"
    - path: "src/components/timeline/TimelineEntry.astro"
      provides: "Single milestone entry with date, title, description, image, link"
    - path: "src/components/timeline/TimelineYear.astro"
      provides: "Year group wrapper with header and visual separator"
    - path: "src/content/timeline/2026-site.mdx"
      provides: "Sample timeline entry for build verification"
  key_links:
    - from: "src/pages/timeline.astro"
      to: "src/components/timeline/TimelineYear.astro"
      via: "year-grouped rendering"
      pattern: "import.*TimelineYear"
    - from: "src/components/timeline/TimelineYear.astro"
      to: "src/components/timeline/TimelineEntry.astro"
      via: "entry rendering within year group"
      pattern: "import.*TimelineEntry"
    - from: "src/pages/timeline.astro"
      to: "src/content/timeline/"
      via: "getCollection('timeline')"
      pattern: "getCollection.*timeline"
---

<objective>
Timeline page (PAGE-11) — vertical alternating-side milestone layout with year grouping, image support, and optional links.

Purpose: The timeline tells the owner's story — key milestones presented in a visually engaging alternating layout that creates rhythm and structure.
Output: Working /timeline page with year-grouped, alternating-side entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-community-search/04-CONTEXT.md
@src/content.config.ts
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: TimelineEntry + TimelineYear + sample content + timeline page</name>
  <files>
    src/content/timeline/2024-start.mdx,
    src/content/timeline/2024-blog.mdx,
    src/content/timeline/2026-site.mdx,
    src/components/timeline/TimelineEntry.astro,
    src/components/timeline/TimelineYear.astro,
    src/pages/timeline.astro
  </files>
  <read_first>
    src/content.config.ts,
    src/layouts/BaseLayout.astro,
    src/pages/works/projects/index.astro
  </read_first>
  <action>
    **1. Create sample timeline entries** in `src/content/timeline/`:

    `2024-start.mdx`:
    ```
    ---
    date: 2024-03-15
    title: 开始学习编程
    description: 从零开始学习前端开发，第一行代码是 Hello World
    ---
    ```

    `2024-blog.mdx`:
    ```
    ---
    date: 2024-09-01
    title: 搭建第一个博客
    description: 用 Hexo 搭建了第一个个人博客，开始记录学习笔记
    ---
    ```

    `2026-site.mdx`:
    ```
    ---
    date: 2026-06-01
    title: 新站上线
    description: 用 Astro 重构了个人站点，二次元风格全面升级 ✨
    link: https://merlinalex.me
    ---
    ```

    These match the timeline schema in content.config.ts: date, title, description, image (optional), link (optional), side (auto by default).

    **2. Create `src/components/timeline/TimelineEntry.astro`:**
    - Props: `entry` (CollectionEntry<'timeline'>), `side` ('left' | 'right')
    - Layout: card positioned on left or right side of a vertical center line
    - Content: date (formatted YYYY-MM-DD), title (bold), description (if present), image (if present, with lazy loading), link (if present, as a styled "了解更多 →" button)
    - The vertical center line: use a `::before` pseudo-element on the parent container
    - Left entries: card aligned right of center line, with a dot/circle on the line
    - Right entries: card aligned left of center line, with a dot/circle on the line
    - Use CSS: `position: relative`, the dot uses `::after` pseudo-element on the entry
    - Responsive: on mobile (<640px), all entries stack on the left side (no alternating)
    - Use CSS variables: `--color-surface`, `--color-border`, `--color-fg`, `--color-accent`, `--radius-md`

    **3. Create `src/components/timeline/TimelineYear.astro`:**
    - Props: `year` (number), `entries` (array of CollectionEntry<'timeline'>)
    - Layout: year header (large text, accent color, centered), followed by entries
    - Year header: styled as a badge or pill, centered on the vertical line
    - Visual separator: horizontal line or spacing between year groups
    - Entries alternate sides: first entry left, second right, third left, etc.
    - Pass computed `side` prop to each TimelineEntry

    **4. Create `src/pages/timeline.astro`:**
    - Import `getCollection('timeline')` to get all entries
    - Sort by date descending (newest first)
    - Group by year: extract year from `entry.data.date`, create Map<year, entries[]>
    - Render year groups using TimelineYear (most recent year first)
    - Use `BaseLayout` with title "时间线 — merlinalex.me"
    - Add page-level CSS for the vertical center line (continuous line through all years)
    - Center line: `::before` on the timeline container, `position: absolute`, `left: 50%`, full height, `border-left: 2px solid var(--color-border)`
    - Responsive: center line moves to left edge on mobile (<640px)

    Note: Nav.astro "时间线" link is enabled in Plan 04-01 Task 2 (consolidated to prevent file conflicts).
  </action>
  <verify>
    <automated>pnpm exec astro build 2>&1 | tail -5 && test -f dist/timeline/index.html && grep -c "时间线" dist/timeline/index.html && echo "BUILD OK"</automated>
  </verify>
  <done>
    - /timeline page renders with year-grouped entries
    - Entries alternate left/right within each year
    - Year headers display prominently
    - Sample entries show date, title, description
    - Optional link renders as "了解更多 →" button
    - Mobile layout stacks entries on left
    - Vertical center line visible between entries
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content files → page render | Markdown frontmatter validated by Zod schema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | timeline content | mitigate | Zod schema validates date, title, description, link (URL validation) |
</threat_model>

<verification>
1. `pnpm exec astro build` succeeds
2. `/timeline/index.html` exists in dist
3. Year grouping visible in rendered HTML
4. Alternating left/right layout visible (check CSS classes)
5. Sample entries render with dates and titles
</verification>

<success_criteria>
- Visitor can scroll /timeline with year-grouped alternating milestones
- Entries show date, title, description, optional image, optional link
- Mobile layout works (single column)
- Build completes without errors
</success_criteria>

<output>
Create `.planning/phases/04-community-search/04-03-SUMMARY.md` when done
</output>
