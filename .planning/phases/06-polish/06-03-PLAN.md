---
phase: 06-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - vitest.config.ts
  - playwright.config.ts
  - .github/workflows/test.yml
  - src/utils/__tests__/word-count.test.ts
  - src/lib/__tests__/atmosphere.test.ts
  - src/lib/__tests__/json-ld.test.ts
  - e2e/theme-persistence.spec.ts
  - e2e/search.spec.ts
  - e2e/reduced-motion.spec.ts
  - e2e/live2d-fallback.spec.ts
  - e2e/bgm.spec.ts
  - e2e/comments.spec.ts
  - docs/build-hardening.md
autonomous: true
requirements:
  - TEST-01
  - GIT-01

must_haves:
  truths:
    - "Running pnpm test runs Vitest with coverage and exits 0"
    - "Running pnpm test:e2e runs Playwright against 6 critical paths and exits 0"
    - "Coverage report shows >=80% on critical paths"
    - "GitHub Actions workflow runs on push/PR and fails if coverage drops below 80%"
    - "Build hardening env vars are documented for Cloudflare Pages"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest config with coverage thresholds"
      contains: "coverage"
    - path: "playwright.config.ts"
      provides: "Playwright E2E config"
      contains: "projects"
    - path: ".github/workflows/test.yml"
      provides: "CI workflow for tests + coverage gate"
      contains: "vitest"
    - path: "docs/build-hardening.md"
      provides: "Cloudflare Pages env var documentation"
      contains: "NODE_OPTIONS"
  key_links:
    - from: "vitest.config.ts"
      to: "src/utils/*.ts"
      via: "test include pattern"
      pattern: "src/\\*\\*/\\*\\.test"
    - from: "playwright.config.ts"
      to: "localhost:4321"
      via: "webServer config"
      pattern: "webServer"
    - from: ".github/workflows/test.yml"
      to: "vitest.config.ts"
      via: "runs pnpm test"
      pattern: "pnpm test"
---

<objective>
Establish test infrastructure with >=80% coverage on critical paths and CI gate, plus document build hardening for Cloudflare Pages.

Purpose: Phase 6 is the final polish phase. This plan ensures the site has automated test coverage that catches regressions before deploy, and documents the build optimizations needed for reliable Cloudflare Pages builds.

Output: Vitest config with coverage thresholds, Playwright E2E tests for 6 critical paths, GitHub Actions CI workflow, build hardening documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-polish/06-CONTEXT.md
@vitest.config.ts
@package.json
@src/lib/atmosphere.ts
@src/utils/word-count.ts
@src/utils/reading-time.ts
@src/utils/tag-extraction.ts
@src/utils/related-posts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vitest config + unit tests for utilities</name>
  <files>vitest.config.ts, src/utils/__tests__/word-count.test.ts, src/lib/__tests__/atmosphere.test.ts, src/lib/__tests__/json-ld.test.ts</files>
  <behavior>
    - word-count: counts Chinese characters + English words correctly, handles empty string, handles mixed content
    - atmosphere: isOff returns true when window.__atmo__ is undefined, isOff returns true when level is 'off', isSubtle returns true when level is 'subtle', isFull returns true when level is 'full', isReducedMotion returns true when reducedMotion is true, getLevel returns current level, onAtmoChange subscribes and unsubscribes
    - json-ld (from 06-01): generates valid Article schema, generates valid Person schema, generates valid BreadcrumbList schema
  </behavior>
  <action>
Update `vitest.config.ts` to add coverage configuration:

```typescript
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    include: ['src/**/*.{test,spec}.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      include: ['src/utils/**/*.ts', 'src/lib/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/**/*.spec.ts'],
      thresholds: {
        statements: 80,
        branches: 80,
        functions: 80,
        lines: 80,
      },
    },
  },
});
```

Create `src/utils/__tests__/word-count.test.ts` with tests for the `wordCount` function:
- Empty string returns 0
- English text counts words correctly
- Chinese text counts characters correctly
- Mixed English + Chinese counts both
- Handles whitespace-only input

Create `src/lib/__tests__/atmosphere.test.ts` with tests for the atmosphere gate utility:
- Mock `window.__atmo__` for each test
- `isOff()` returns true when `window.__atmo__` is undefined
- `isOff()` returns true when level is 'off'
- `isSubtle()` returns true when level is 'subtle'
- `isFull()` returns true when level is 'full'
- `isReducedMotion()` returns true when `reducedMotion` is true
- `getLevel()` returns current level
- `onAtmoChange()` subscribes and receives state changes
- Unsubscribe function removes listener

Create `src/lib/__tests__/json-ld.test.ts` (depends on 06-01 creating the JSON-LD utility):
- Test Article schema generation with required fields
- Test Person schema generation
- Test BreadcrumbList schema generation
- Validate output matches expected JSON-LD structure

Run `pnpm test` to verify all unit tests pass with >=80% coverage on tested paths.
  </action>
  <verify>
    <automated>pnpm test -- --coverage 2>&1 | tail -20</automated>
  </verify>
  <done>
    - vitest.config.ts has coverage thresholds at 80%
    - All unit tests pass
    - Coverage report shows >=80% on src/utils and src/lib
    - pnpm test exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Playwright config + E2E tests for 6 critical paths</name>
  <files>playwright.config.ts, e2e/theme-persistence.spec.ts, e2e/search.spec.ts, e2e/reduced-motion.spec.ts, e2e/live2d-fallback.spec.ts, e2e/bgm.spec.ts, e2e/comments.spec.ts</files>
  <action>
Create `playwright.config.ts` with the following configuration:
- Use `@playwright/test` (already in devDependencies)
- Set `testDir` to `./e2e`
- Configure `webServer` to run `pnpm dev` on port 4321 with `reuseExistingServer: true`
- Set `use.baseURL` to `http://localhost:4321`
- Configure projects for `chromium` only (per CLAUDE.md: Latest Chrome/Safari/Edge)
- Set `retries: 1` and `workers: 1` for CI stability
- Set `reporter: [['html', { open: 'never' }], ['list']]`
- Set `timeout: 30000` per test

Add to `package.json` scripts:
```json
"test:e2e": "playwright test",
"test:e2e:ui": "playwright test --ui"
```

Create 6 E2E test files in `e2e/` directory:

**1. `e2e/comments.spec.ts` — Comment posting (Twikoo island)**
- Navigate to an article page
- Verify Twikoo comment container is present
- Verify comment input is visible (may need to wait for Twikoo iframe to load)
- Note: Actual posting requires Twikoo backend — test that the container loads and is interactive

**2. `e2e/theme-persistence.spec.ts` — Theme persistence**
- Navigate to home page
- Click theme toggle to switch to dark mode
- Verify `document.documentElement.dataset.theme` is 'dark'
- Reload the page
- Verify theme is still 'dark' after reload
- Switch back to light mode
- Verify persistence works for light mode too

**3. `e2e/search.spec.ts` — Search index (Pagefind)**
- Navigate to home page
- Find the search input (Pagefind widget)
- Type a search query (e.g., "test" or a known article keyword)
- Wait for search results to appear
- Verify at least one result is returned
- Verify result links are clickable

**4. `e2e/reduced-motion.spec.ts` — Reduced-motion gate**
- Set `prefers-reduced-motion: reduce` via Playwright's `emulateMedia`
- Navigate to home page
- Verify atmosphere layer is hidden or animations are disabled
- Check that `data-atmo` attribute respects reduced-motion preference

**5. `e2e/live2d-fallback.spec.ts` — Live2D fallback**
- Navigate to home page
- Check if Live2D canvas exists (on capable devices) or static PNG fallback exists
- On low-end device simulation (set `navigator.deviceMemory = 2`), verify fallback is shown
- Verify the fallback image is visible and loads correctly

**6. `e2e/bgm.spec.ts` — BGM unmute**
- Navigate to home page
- Find the APlayer container
- Verify APlayer is present and muted by default
- Click the unmute button
- Verify APlayer state changes to unmuted
- Verify music playback state is correct

Run `pnpm test:e2e` to verify all E2E tests pass.
  </action>
  <verify>
    <automated>pnpm test:e2e 2>&1 | tail -20</automated>
  </verify>
  <done>
    - playwright.config.ts exists with webServer config
    - 6 E2E test files exist in e2e/ directory
    - All E2E tests pass against local dev server
    - pnpm test:e2e exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: CI workflow + build hardening docs</name>
  <files>.github/workflows/test.yml, docs/build-hardening.md, package.json</files>
  <action>
Create `.github/workflows/test.yml` — GitHub Actions CI workflow that runs on push and PR:

```yaml
name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: pnpm test -- --coverage
      - run: pnpm build

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - run: npx playwright install --with-deps chromium
      - run: pnpm test:e2e
```

Update `package.json` scripts to add test commands:
```json
"test": "vitest run",
"test:coverage": "vitest run --coverage",
"test:e2e": "playwright test",
"test:e2e:ui": "playwright test --ui"
```

Create `docs/build-hardening.md` documenting the Cloudflare Pages environment variables needed for build optimization (per D-05):

```markdown
# Build Hardening

## Cloudflare Pages Environment Variables

Set these in Cloudflare Pages dashboard → Settings → Environment variables:

### NODE_OPTIONS
- **Variable:** `NODE_OPTIONS`
- **Value:** `--max-old-space-size=4096`
- **Purpose:** Increases Node.js heap size to prevent OOM during Astro build
- **Scope:** Production + Preview

### Sharp Cache
- **Variable:** `SHARP_IGNORE_GLOBAL_LIBVIPS`
- **Value:** `1`
- **Purpose:** Forces Sharp to use its bundled libvips, avoiding version conflicts
- **Scope:** Production + Preview

## Build Time Budget

Target: <2 minutes for full build (leaves margin within Cloudflare's 20-min timeout).

Monitor build times in Cloudflare Pages dashboard → Deployments → Build duration.

## Verification

After setting env vars, trigger a manual deploy and verify:
1. Build completes without OOM errors
2. Build time is <2 minutes
3. All pages render correctly
```
  </action>
  <verify>
    <automated>cat /Users/huangjingping/i/merlinalex.me/.github/workflows/test.yml | head -5 && cat /Users/huangjingping/i/merlinalex.me/docs/build-hardening.md | head -5</automated>
  </verify>
  <done>
    - .github/workflows/test.yml exists with test + e2e jobs
    - docs/build-hardening.md documents NODE_OPTIONS and Sharp cache env vars
    - package.json has test, test:coverage, test:e2e scripts
    - CI workflow runs on push/PR to main
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → production | Tests must pass before deploy; coverage gate prevents regressions |
| E2E → local dev server | Playwright tests run against local dev server, not production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-06-03-01 | Tampering | CI workflow | accept | Workflow runs in GitHub Actions sandbox; no secrets exposed in logs |
| T-06-03-02 | Elevation | test coverage gate | mitigate | Coverage threshold at 80% prevents merging untested code |
</threat_model>

<verification>
- `pnpm test` runs Vitest and exits 0
- `pnpm test:coverage` shows >=80% on critical paths
- `pnpm test:e2e` runs Playwright and exits 0
- `.github/workflows/test.yml` exists and defines test + e2e jobs
- `docs/build-hardening.md` exists with env var documentation
- `package.json` has test, test:coverage, test:e2e scripts
</verification>

<success_criteria>
All 6 critical paths have E2E tests. Vitest unit tests cover utilities with >=80% coverage. CI workflow runs on push/PR and fails if coverage drops below 80%. Build hardening env vars are documented for Cloudflare Pages.
</success_criteria>

<output>
Create `.planning/phases/06-polish/06-03-SUMMARY.md` when done
</output>
